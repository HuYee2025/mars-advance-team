# 当前交接记录

更新时间：2026-07-08

## 用途

这是新对话进入项目时的快速交接入口。若同一对话发生约 3 次自动压缩，先检查本文件和相关主管文档是否完整；完整后建议开启新对话继续。

新对话默认先读：

1. `AGENTS.md`
2. `docs/project-management.md`
3. `docs/production-blueprint.md`
4. `docs/dev-log/current-handoff.md`
5. `docs/tech-plan.md`
6. `docs/decision-log.md`

专项交接：

- 虫洞、远古巨树拱门与飞行背包：`docs/dev-log/handoff-2026-06-27-wormhole-jetpack.md`

## 最近已完成的重点

- 2026-07-08 激光剑未获得状态不再静默：玩家在获得激光剑前按 `I` 或 `J`，会弹出“尚未获得激光剑。完成远古巨树拱门的时空之门穿越，坠落回火星后即可获得。”，并带约 `2.2s` 冷却避免长按刷屏。获得后仍保持 `I` 展开/收回、按住 `J` 举起的原逻辑。
- 2026-07-08 已删除火星足球的独立足球网/球门实体，足球奖励判定改为“足球穿过远古巨树拱门门洞”。首次穿过门洞仍一次性解锁相机，后续穿过门洞奖励 `+100` 积分并把足球重置回出生点；相机未获得提示同步改为“把足球踢进古树拱门门洞”。虫洞白色粒子过渡改为无文字版本：不再显示“这是……”，进入白场时已经初始化同一个有宇航员的虫洞镜头，宇航员保持飞行姿态漂浮在白色粒子虚空中约 `3s`，随后粒子快速四散并直接露出黑色虫洞空间，避免出现短暂没有宇航员的虫洞空镜。
- 2026-07-08 继续修正远古巨树拱门下方透明碰撞体：用户截图坐标约 `X +042 / Z +065`，复算发现旧 `远古巨树拱门根部 localX 33 / localZ -43` collider 位于可见实体之外并会卡住蜘蛛。`src/world.ts` 的 `addAncientTreeArchColliders()` 已改为只覆盖左右门柱和可见低矮根脚的地面 footprint，不再把上方拱形轮廓投影到地面，也删除门洞下方额外根部 collider。
- 2026-07-07 已移除宇航员脚下旧的半透明假接触阴影，避免在真实 shadow map 之外再出现不自然的人形黑斑。火星蜘蛛新增红眼攻击逻辑：发现地面宇航员时眼点由绿变红并主动爬向玩家，触碰按 `20 * damageFactor` 扣体能，体能归零从居住舱重生；飞行状态免疫，激光剑展开时蜘蛛回避且不造成伤害。缩放枪现在可以锁定红眼火星蜘蛛，放大或缩小会同步改变它的触碰伤害倍率，效果过期后恢复。
- 2026-07-07 已按实机观感回退两个新增 shader 贴图层：`src/main.ts` 删除沙尘暴 `stormWindLayer` 球幕风带和虫洞坠落全屏 `shaderVeil`。沙尘暴恢复为原有 fog、背景色变化、旧沙尘粒子和 `stormStrength` 逻辑；虫洞坠落恢复为原有粒子、黑洞、有机隧道、火星坠落和旧 veil 贴图，人物操作不变。远古巨树拱门时空之门能量膜 shader 继续保留。
- 2026-07-07 已取消刚加的半透明椭圆假投影层，改用跟随玩家附近区域的真实 Three.js shadow map：太阳光源和 shadow target 会围绕玩家附近更新，优先覆盖玩家周边的飞船、建筑、远古巨树拱门、黑色方碑、NASA 火星车和近火巨型流星主体。太阳 sprite 保留 `depthTest`，不再透过飞船/建筑显示。远古巨树脚部和根部 collider 已收紧，减少门洞脚下出现透明阻挡。
- 2026-07-07 已修正暗面坠毁飞船残骸碰撞和透明空气墙：残骸 collider 不再用正面 `planetNormal(x,z)` 重新投射，而是从 `CRASHED_SHIP_SITE_NORMAL` 按残骸局部切线生成，避免实际残骸可穿过、另一侧坐标出现空气墙。火星蜘蛛同步增加独立活动偏移、目标分离、位置分离和更大的动态身体 collider，减少多只蜘蛛叠成一团或集体同向移动的问题。
- 2026-07-06 已把 shader 文章的部分方法应用到游戏：`src/world.ts` 将远古巨树拱门时空之门能量膜改为流动 shader，并增加门内深度 veil，原粒子、电弧和真实点光源保留；远处太阳已从可见几何球壳改为发光 sprite 光斑，远景是接近纯白的高亮光源和外发光。缩放枪放大到 `2x` 和 `3x` 时仍只放大默认发光太阳，不叠加近景贴图；到接近 `4x` 时才用默认太阳白色光盘作底，并在上方覆盖 `src/assets/sun-close-detail-texture.png` 透明近景太阳图。该贴图使用用户提供的圆形透明太阳主体图，只保留太阳圆盘和贴身亮边，不带周边日冕喷溅；底层白盘复用默认太阳光盘纹理，尺寸略大于贴图，用于填补边缘漏光。太阳 sprite 已调整为绘制在星空点层之上，避免放大后背景星点压到太阳表面。此前尝试的 canvas 动态火纹层因产生灰点和额外纹理更新开销已移除。近距贴图不自转，远景射线、大面积朦胧光晕、均匀圆环和额外 procedural 日冕层全部收掉。放大倍率同时增强太阳 `DirectionalLight` 和全屏过曝，接近 `4x` 时模拟火星表面亮到难以直视。太阳达到 `3x` 以上时，体能会按太阳当前倍率持续消耗，室内、对话、乘车和电梯状态也生效，太阳恢复默认大小后停止。缩放枪锁定框只显示目标名称和一到两行知识简介，真实目标使用百科/官方数据，游戏目标结合真实参考物和项目设定；锁定有效目标或切换目标时播放一次短促“滴滴”扫描音，底部按钮显示 `缩小 Q` / `放大 E`。蜘蛛区贴地熔岩/粒子云效果因近看像平面贴图已取消；蜘蛛现在按真实太阳方向选择古树背光侧活动，并重做了更明显的八足交替爬行动作。虫洞全屏 shader veil 和沙尘暴 shader 球幕风带曾短暂尝试，已在 2026-07-07 因贴图感明显回退。已通过 `pnpm build`。
- 2026-07-06 已阅读技术文章 `Three 学习资料/一个 Shader 生成整个世界：Three.pdf`，并沉淀为 `docs/research/shader-world-threejs-procedural-scene.md`。结论：文章中的 `Ray Marching`、`SDF`、`fBm`、shader 内粒子、体积辉光、距离雾和 film grain 值得借鉴，但只用于虫洞、时空之门、沙尘暴、太阳火纹、暗面异常和对话/故事背景等特殊视觉层；不替换当前以 Three.js 实体、碰撞、雷达、任务和多人同步为核心的主世界。
- 真实太阳光照 MVP 已接入：新增 `src/mars-sun-model.ts`，按真实 `Date.now()` 和 NASA Mars24 近似算法计算火星日下点方向、`Ls`、MTC、火星日心距离和太阳辐照强度；`src/main.ts` 里的太阳 `DirectionalLight`、可见太阳、太阳能阵列追日和暗面蜘蛛避光已统一使用该真实太阳方向。太阳光照不再使用 `10x` 游戏时间加速；沙尘暴作为玩法例外，仍使用 `STORM_TIME_SCALE = 10` 加速推进。已通过 `pnpm build`。
- 2026-07-04 研究 `hyqzz/Solar-Wanderer` 后形成外部数据利用结论，详见 `docs/research/solar-wanderer-mars-data-lighting.md`：该仓库对本项目最值得借的是真实太阳位置、火星自转、太阳光照方向、大气散射和火星车真实地标坐标；不建议直接把其 `public/textures/mars.jpg` 当作近地表真实火星图片，也不建议直接依赖仓库内 DEM URL。下一步若推进真实化，优先做“真实太阳光照 MVP”，火星表面图片另从 NASA/HiRISE/PDS/USGS 选定真实来源。
- 正式剧情对话舞台背景已从纯暗色渐变改为暗红火星局部纹理：新增 `src/assets/dialogue-mars-surface-bg.webp`，从火星球体贴图 `src/assets/mars-albedo-generated.webp` 裁切压缩，保持卫星俯拍质感；最新版本降低模糊和遮罩强度，让火星纹理可见但不抢角色和对话框。
- 雷达 HUD 布局已简化：雷达内不再显示 `N/E/S/W` 方位字母；`坐标/Position` 和 `方位/HDG 000°` 朝向读数固定排成一行，放在右上角金币、积分和 `F` 按钮正下方。该读数现在独立于雷达开关实时更新，样式对齐左侧“制氧量”等指标标签的字色和字号。
- 史蒂夫信息接收改为低打扰：收到信息时不再自动弹出“是否接收”的信息栏，也不显示 `稍后接收/接收` 选项；只让顶部 `R` 按钮发红光，不再把当前任务改成“按 R 查看”。玩家按 `R` 后直接进入史蒂夫对话；对话完成后再按 `R` 才显示当前任务详情。顶部 `V/R/F`、金币、积分的普通打开态和奖励跳动不再做外发光，只有 `R` 有未读信息时保留红光提示。
- 坠毁飞船残骸碰撞已从中心整体圆形 collider 改为按两段船体和端部碎片分段布置；断裂中间不再阻挡玩家、车辆或足球，避免挡住接近福福的路线。2026-06-30 复查截图坐标 `X -020~-079 / Z -136~-185` 后，确认空气墙来自残骸分段 collider 边缘轻微外扩，已把船体和端部 collider 半径收紧，保留实体阻挡但让残骸周边地表可通行。
- 新增激光剑装备：不是初始装备；玩家首次穿越远古巨树拱门的时空之门并坠落回火星后一次性获得，随后弹出获得提示和 `I/J` 使用教程。获得前按 `I/J` 会提示获取地点；获得后按 `I` 展开/收回，平时不显示。展开时握在亚历克斯右手，白色剑身带微蓝泛光，并用独立 scene 点光源照亮附近暗面区域，第一人称隐藏角色模型时光照仍然有效。按住 `J` 会把右臂向前上抬约 `90°` 举起激光剑，松开回到正常握剑；普通持剑光照强度已提高约 `30%`，举剑时光照强度和照明半径变为普通持剑的 `2` 倍。激光剑和缩放枪互斥，按 `X` 调用缩放枪时会收起激光剑，按 `I` 调用激光剑时会退出缩放枪瞄准；缩放枪获得后也不再常驻手上，只在按 `X` 瞄准时显示。
- 碰撞修正：暗面蜘蛛已加入动态圆形 collider，玩家不能穿过蜘蛛身体；远古巨树拱门两侧实体和根部的球面 collider 已加密，减少从实体侧面穿模，同时保留门洞中线通行。
- 暗面蜘蛛设定已改为时空之门来客：火星蜘蛛没有巢穴、地下洞穴或黑色洞口；旧巢穴模型、洞口 collider、雷达未知点和 `+100` 隐藏发现积分已删除。原巢穴地形下陷已从高度函数中移除，活动区不再生成碗形坑；蜘蛛改为围绕远古巨树和坠毁飞船之间的时空之门活动区分散巡游，并降低中心附近岩石密度，减少原地卡住。3 只低多边形暗面蜘蛛不接任务、不进多人同步；移动时避开建筑和飞船实体，遇到岩石会抬高身体爬过，遇到玩家手持或举起激光剑会主动避开。蜘蛛平时保留绿色眼点和腹部后半截绿色发光点，发现可触碰的地面宇航员后眼点变红并触发接触伤害。
- 2026-07-04 复查透明空气墙和蜘蛛原地爬行：远古巨树拱门 collider 已确认使用 `localX/localZ` 球面投射，避免左右实体叠到中轴；坠毁飞船残骸 collider 半径收紧到可见船体核心，截图坐标 `X -008 / Z -154` 前方不再被端部 collider 贴边挡住。蜘蛛活动中心从靠近拱门的位置移到更靠坠毁飞船一侧的开阔区，巡逻半径收窄；强光下不再直接回到 home 点静止，而是保留小范围慢速巡游，腿部动画在无位移时降为低速 idle。
- 车辆路线修正：两辆可搭乘车辆仍走原 `greatCircleLoop` 大圆路线，但路线附近地形会被局部平滑，随机岩石生成会避开路线安全带；远古巨树拱门脚部 collider 继续阻挡玩家/足球，但车辆路线避障会忽略这些 collider，防止车辆在古树巨洞脚部卡住。
- 维修机器人避障修正：机器人巡逻点生成时已远离固定 collider，但仍可能在等待或移动中贴进建筑外壳；现在初始化、等待和移动三个状态都会重新执行 `keepBotPointOutsideFixedColliders`，并把机器人专用建筑安全距离提高到 `5.8`，被判定阻塞的距离提高到 `5.2`，减少维修机器人进入建筑体。
- 首页小屏样式微调：中文主标题在小屏放大约 `20%`，但字重降低约 `30%`；“终于，火星基地迎来了第一位人类居民。”和下方两个入口按钮缩小约 `30%`，减少遮挡火星画面。
- 虫洞坠落期间已隐藏普通金币实体，并同步隐藏雷达上的金币标记；金币刷新和拾取在虫洞期间暂停，避免金币穿进虫洞画面。虫洞开始后 body 会进入 `is-wormhole` 状态，隐藏 HUD、雷达、任务、互动、标签、对话和移动端按钮等 2D 界面，只保留虫洞 WebGL 画面与人物。
- 居住舱照片展示已从 `7` 个实体相框改为一个留边的大型电子屏；没有照片时显示橙红色英文祝福待机画面，有照片时每约 `4s` 自动循环展示，避免室内再出现旧相框和黑色大块。
- 温室生态舱新增整体圆形外壳 collider，覆盖穹顶/地台范围，阻止玩家从任意位置直接走进温室；只有按 `E` 通过温室互动进入后，该整体外壳 collider 才随 `greenhouseDoor.occupied` 临时关闭，离开温室后恢复。温室任务互动半径已同步放大到碰撞外沿，避免主线触发被外壳 collider 挡住。
- 背景音乐新增 `B` 键播放/停止切换；进入游戏默认播放，虫洞坠落期间仍强制静音，并保留玩家当前的音乐开关状态。操作指南已补 `B / 背景音乐`。
- 新增音频创作提示词文档 `docs/audio-prompts.md`：整理开场主题、平时探索、暗面、远古巨树/时空之门、虫洞、蜘蛛危险、温室/福福、通信塔、风暴、埃隆高空廊道、结局，以及缩放枪和激光剑等短音效 prompts；原则是原创生成，不直接采样或提示现成电影/IP 声音。
- 故事概要页已同步最新角色命名：简体中文可见文本中的 `Alex` 改为“亚历克斯”，基地总控 AI 从 `Mother` 改为“史蒂夫”；英文故事概要中的中央 AI 名称同步改为 `Steve`。同步范围包括线上页面 `public/story-overview.html` 和文档源页 `docs/story/core-story-v2-visual.html`。
- 多人 WebSocket 服务已清理头盔灯遗留字段：`server/multiplayer.ts` 不再在默认玩家快照和快照归一化中写入已移除的 `lampOn` 字段。
- 首页火星日期改为 ARES 火星历动态日期：以 `2026-06-27 00:00 Asia/Shanghai` 对应 `2050 年 / 第 30 火星日` 为锚点，每 `88775.244` 秒推进一个 sol，每 `669` sols 进位一年。
- 首页语言切换只在首页可用；进入游戏后隐藏。按钮已改为右上角约 `24px` 的立体自转国旗小球：中文界面显示美国国旗，英文界面显示中国国旗。
- 首页已补首屏 critical CSS，避免本地预览服务器或慢网打开时短暂出现白底黑字的未样式化 HTML；语言切换小球改为在 24px 圆形内部滚动 background-position，避免 Safari 把旗面动画渲染成小圆柱。
- 首页两个主按钮去掉浏览器蓝色焦点框，改为和游戏内顶部按钮一致的暗底、棕金边框、红色静态发光交互状态。
- 首页标题样式微调：中文首页大标题小屏更大但更细；英文首页在 `520px` 以下小屏按三行显示为 `Mars Advance` / `Team` / `The First Human`，避免字母溢出屏幕。
- 首页入口按键已统一为 `E/Q`，且标题、说明和入口按钮在宽屏/窄屏都水平居中排列：默认选中“进入基地”，首页按 `E` 进入基地，按 `Q` 进入故事概要；`W/S` 或方向键、鼠标悬停只切换视觉选中项，不再用 `Enter/Space` 确认首页按钮。
- 剧情对话框桌面宽度从约 `70vw` 收窄到约 `50vw`；说话角色立绘改为贴近对话框左右端。开局任务提示改成更具体的按键说明：`W/S/A/D` 探索、`Space` 跳跃、`Shift` 加速。桌面对话支持 `Q` 返回上一句；出现两个并列选项时仍是选择优先，`Q` 选左、`E` 选右。
- 右上角金币和积分改为按钮化 HUD：金币初始 `$`，积分初始 `0`，获得后用三位数字显示。
- `V`、`R`、`F` 顶部按钮统一视觉：关闭态半透明深色底；打开态只改边框和文字，不做外发光；只有 `R` 有未读任务或史蒂夫信息时动态红光提示。
- 小屏任务提示改为 `R` 按钮开关；移动端 `R` 与 `F` 互斥，桌面端可以同时打开。进入游戏时宽屏/桌面默认打开普通雷达，小屏或触摸端默认关闭雷达。
- 小屏剧情对话框固定高度并上移；正文最多三行，超出自动分页；底部 `继续/选项` 按钮固定在面板底部按钮区，不随正文高度跳动。
- 移动端双选项对话使用 `A/B` 短按钮，完整选项文字写入正文。
- 对话按钮点击、翻页和确认播放与首页“进入基地”一致的短促 UI beep。
- 头盔灯/照明功能已移除。
- 飞行作弊模式已调整：下降可落地并退出飞行背包；飞行中取消走路动作，改为腿部轻微摆动；可落在飞船高处走廊等可行走表面。
- 太阳能阵列 A/B/C 已移动到更靠近 `03 飞船 返回飞船` 一侧的首页亮面区域，源码坐标为 `(-22,52) / (-4,52) / (14,52)`，并同步管线、维修机器人、碰撞体、雷达和互动点。
- `ARES Starlink-M` 火星低轨通信星座已改为固定轨道星座：进入游戏后天空直接存在 `5` 组星链，每组 `7` 颗，共 `35` 颗；取消每 24 个火星日新增卫星的按时释放任务，不做释放动画。
- 星链卫星按 5 串编队飞过天空，每串 7 颗，分布在不同轨道倾角和升交点经度；为便于肉眼识别，轨道高度已改到高于近火流星轨道，单颗实体代理约放大 `5` 倍，并改成白色自发光圆形微光。
- 星链卫星有可见实体模型和不可见缩放枪命中体；通信塔校准后任务提示和雷达显示轨道链路状态。
- 缩放枪可锁定并命中每颗星链卫星模型；每颗卫星首次命中奖励隐藏发现积分。
- 缩放枪也可锁定近火轨道的那颗石质流星本体；该近火流星已改成绕火星的固定圆形近火轨道，轨道半径恒定为 `PLANET_RADIUS + 150`，也就是高出火星地表约 `150` 游戏单位，约等于三艘飞船叠加高度；它从玩家初始基地上空轨道点开始，每现实约 `90` 秒绕行一圈并回到同一上空轨道点，不再使用容易产生贴地错觉的掠过高度曲线。近火流星头部不再使用蓝色气体罩、蓝色气体尾巴或蓝色自发光，本体材质为不透明深灰石质。近火流星会在雷达上显示为蓝色点；命中后流星会按缩放枪倍率变化 60 秒，首次命中奖励隐藏发现积分。其它两颗远处天体不参与缩放枪判断。
- NASA 机遇号火星车遗迹已移到更空旷的位置，源码坐标约 `(-300, 0)`，远离三艘飞船和基地车辆大圆路线；贴地高度已下调，轮子应落到火星地表。该遗迹仍作为隐藏问号目标，首次接近发现奖励 `+50` 积分。
- 游戏正式对话已排查并移除所有三选项节点，最多只保留两个选择，以匹配桌面端 `Q/E` 二选一操作。
- 雷达标记改为分类清晰：任务目标黄色，并带一圈圈放大渐隐的黄色雷达光圈；氧气补给绿色；金币为小 `$` 金币样式且不发光。
- 雷达中的近火流星标记改为深灰岩石色，带浅灰描边，和轨道星链/蓝色点区分开。
- 背面黑暗半球的“远古巨树拱门”已完成选址和尺度草案，见 `docs/site-planning/ancient-tree-arch-placement.md`；最新推荐位置改为用户在地图上标注的黑暗半球中央点，约 `lon 0° / lat -50°`，对应背面 `X 0 / Z 111`，总高约 `118` 游戏单位。旧候选点 `X 210 / Z 260` 保留为备用。最新准确地图为 `docs/site-planning/mars-fixed-objects-accurate-latlon-cn.svg`：由源码实际 `X/Z` 与半球 normal 计算经纬度生成，并带基地局部等比例放大图；旧版 `mars-layout-flat-map.svg` 和 `mars-layout-fixed-objects-cn.svg` 仅作草图参考。
- 远古巨树拱门已补草图和基座范围图：`docs/site-planning/ancient-tree-arch-sketch-and-footprint.svg`。当前建议总高 `118`、外宽 `92`、主洞 `42 x 78`、基座预留半径 `58`、碰撞半径 `58-64`。
- 远古巨树拱门已实现进 `src/world.ts`：模型是一体化石化树桩形态，底部扩张、向上收缩，中间洞口保留可通行空间；左右实体和根部已加入背面球面 normal 碰撞体，实体部分不可穿过，洞口中线铺设深色道路供人和车辆通过。`src/main.ts` 已补 `normalForCollider`，玩家、足球、相机遮挡和车辆避障都可识别背面经纬度碰撞体。
- 远古巨树拱门中间坡道/道路已移除，门洞中心直接走火星星球地表，减少额外路面与角色脚底高度不一致的问题。
- 远古巨树拱门门洞底部已取消门槛：主体几何从“整块外形 + 内部洞”改为底部开放的 U 形轮廓，中心通道直接落到火星地表，不再有可钻入的底部横向实体。
- 远古巨树拱门主体沿地表法线下沉 `10.2` 游戏单位，用于把两侧外扩尖角埋入火星土里；中间通道不再铺设独立道路。
- 远古巨树拱门门洞内的时空之门不再定时自动打开；金币不足 `50` 时靠近门洞不弹支付信息。金币足够时，玩家进入门洞约 `1.2x` 判定区会弹出“你是否愿意为穿越时空之门支付50个火星币？”，飞行背包状态下只要处在门洞投影上方的同类范围内也可触发支付邀请，并提供 `考虑一下(Q)` / `支付(E)`；选择“考虑一下”后必须离开判定区再进入才会重新弹出，选择“支付”后扣 `50` 金币并开启约 `28.5s`。开启后中心为白色能量膜，边缘蓝色光晕，并带动态粒子和随机电弧闪电；光效期间会开启多组蓝白色真实点光源，并叠加门柱内壁与地面的蓝色受光层，让门洞、拱门内侧和附近地面明显被照亮；该效果不参与碰撞。
- 远古巨树拱门星门光效激活时，玩家必须穿过发光门的中轴核心区才会进入白色粒子云过渡，靠近门洞边缘不再触发：白色虚空里不显示文字，宇航员保持飞行姿态漂浮约 `3s`，随后粒子快速向四周散开并直接露出同一个有宇航员的黑色虫洞镜头。虫洞坠落约 `30` 秒不可取消，暂停普通移动、互动、喷气背包、碰撞任务触发、氧气消耗和背景音乐，虫洞期间保持寂静；`A/D` 和方向键左右控制横向漂移，`W/S` 改为前后透视控制。最新手感改为带阻力的漂浮控制：按住 `W/S` 后深度速度逐步变化，不再突进突退，但黑洞几何体变化、粒子流动和旋转保持默认速度，不受 `W/S` 前进或后退影响；横向控制中心会轻微周期性波动并叠加小扰动力，让上下左右移动带不稳定漂浮感。虫洞期间 `E` 暂停黑洞下落进度，`Q` 继续下落。虫洞期间隐藏真实火星世界，强制从第三人称开始，使用深黑太空背景和纯电影镜头：前约 `20s` 以宇航员居中略偏下为主体，默认位置比上一版上移约 `10%`，人物尺寸比上一版虫洞镜头缩小约 `70%`，主背景为算法生成的有机循环隧道，默认视觉前进、粒子流动和旋转速度约为上一版 `2x`。后约 `10s` 出现干净的红色火星俯视尘土地表并快速放大，最终无伤安全落回出生点；每次完成穿越奖励 `+200` 积分。
- 远古巨树拱门雷达显示改为隐藏发现：未靠近前显示黄色 `?`，全屏雷达标签为“未知巨型结构”；玩家靠近约 `76` 游戏单位后解锁为“远古巨树拱门”，雷达标记改为专属石灰灰蓝色 `ancient` 类型。
- 雷达曾新增 `N/E/S/W` 方位环和雷达下方朝向读数；当前已改为雷达内不显示方位字母，坐标和朝向读数固定显示在右上角按钮行下方。
- 神秘物体、未知生命、黑色方碑和 埃隆 只在未发现或未对话前显示 `?`；发现或对话后雷达移除 `?`，改用对应分类圆点和真实名称。
- 长按 `F` 改为临时全屏雷达：背景虚化、游戏世界继续运行，但键盘/人物操作焦点全部交给雷达，人物不可移动也不可触发普通互动；松开 `F` 后恢复到长按前的普通显示状态。全屏雷达期间 `Q` 缩小显示更大区域，`E` 放大显示更小区域，圆点和文字随放大轻微变大。
- 喷气背包飞行期间不再触发普通互动任务，也不弹出互动选择；飞行上升键改为 `E`，下降键改为 `Q`，原来的 `Space` / `Control` 不再控制升降，`Space` 只在非飞行状态用于跳跃。
- 触屏/小屏新增两个移动端操作：双击游戏画面可在第一人称和默认第三人称之间切换；模拟摇杆按 `上 上 下 下 左 左 右 右` 的方向序列松手输入，可触发飞行背包作弊码并播放同键盘作弊码一样的“叮”声。飞行背包开启后，移动端“跳”按钮变为“上升”，“加速”按钮变为“下降”；飞行中仍屏蔽普通互动任务，但可以触发远古巨树拱门虫洞。
- 飞行背包密码已拆成两种来源：`HUYEE` 临时召唤喷气背包，落地或重生后消失，不显示 HUD 能量；同时会立即获得缩放枪和激光剑。`上 上 下 下 左 左 右 右` 通过键盘方向键、`WASD` 或触屏摇杆解锁常备飞行背包装备，HUD 在“体能”下方显示“飞行背包 / Jetpack”能量。常备装备飞行时能量缓慢下降，耗尽会安全落地；每次落地后都从当前电量到 `100%` 固定用约 `10s` 线性恢复，HUD 不再用向上取整提前显示满格，之后可用双击 `Space` 或双击触屏“跳”再次启动。
- 飞行背包操作提示改为一次性：第一次启动飞行时显示桌面端 `W/S/A/D`、`E` 上升、`Q` 下降或触屏端摇杆/按钮说明；常备装备后续双击 `Space` 或触屏双击“跳”再次起飞时不再重复弹提示。代码里新增 `shownOperationHelpIds`，自动操作帮助提示应通过一次性入口显示。
- 新增金币秘籍：键盘输入 `money`（不区分大小写）会触发 `+100` 金币动画；右上角金币读数按每次 `+1` 快速跳动到目标值，未播完时再次输入会继续叠加。
- 维修机器人对话改为单次提示：每个机器人正式对话一次后，会记录到 `talkedRobotIds`，之后该机器人不再进入互动框，也不再重复触发靠近问候；新开一局时该记录随任务状态清空。
- 氧气补给重新规划为分散式：低氧雷达绿色点只提示飞船类氧气源，包括三艘飞船和坠毁飞船残骸；居住舱、温室和氧气生产站不再作为外部补氧点显示。补氧互动仍只在氧气进入 `20%` 报警区间后出现，并且必须近距离贴近飞船实体；三艘飞船外壳和坠毁飞船残骸已补充整体碰撞，避免穿模和远距离触发。
- 体能/氧气规则改为探索导向：走路时体能基础消耗为氧气数值下降速率的 0.5 倍，并乘以 `1.75` 手感系数；加速时体能为走路 2 倍、氧气为走路 1.5 倍；跳跃移动体能为走路 0.5 倍；原地站立缓慢恢复体能但氧气继续下降；进入或离开居住舱、进入温室会满氧满体能；成功搭便车也会立即补满氧气和体能，乘车期间不消耗。
- 氧气消耗已再降低 `30%`：宇航服基础氧气消耗从 `0.42/s` 调为 `0.294/s`，加速仍为基础值 `1.5` 倍；体能消耗保持原手感，不随氧气这次下调一起变慢。
- 第三人称相机已关闭普通行走过程中的自动碰撞避障抬升。此前相机在镜头射线接近岩石、建筑等碰撞体时会沿地表法线抬高，导致出生点和部分地形附近出现视角突然升高；现在普通游戏过程使用稳定第三人称高度，出舱/电梯/室内等明确场景镜头逻辑保留。
- 首次把火星足球踢进远古巨树拱门门洞会一次性解锁相机奖励，弹出三行奖励提示：`恭喜！你获得了一台相机`、`随时按 G 键可以使用相机`、`照片会展示在居住舱的墙上`。相机不建模；按 `G` 打开/收起取景框，取景时人物不移动，`A/D` 转向，`W/S` 缩放，`E/Enter` 拍照。
- 居住舱内部照片展示改为大型电子屏；拍照后缩略图写入电子屏并自动循环展示，最多保留 `36` 张，超过后替换最旧照片。进入居住舱且已有照片时，靠近电子屏会提示“这里保存着你拍下的火星瞬间，按 E 查看。”；按 `E` 打开约 `90vw` 宽的大图查看器，`A/D` 循环切换，`W` 放大，`S` 恢复，`Q` 退出，`E/Enter` 触发当前照片 PNG 下载。浏览器版本不静默保存到桌面。
- 居住舱内部按截图意见做了可读性修正：电子屏前方重叠的深色装饰块已删除；休眠舱不再叠加额外立方体或外伸结构，而是把同一个 capsule 外壳按水平裁切面分材质，下半部分为实体，上半部分为半透明玻璃，下半实体新增同材质横截面盖板，透过玻璃看到的是实体切面而不是地面，并避免上下材质交界出现三角形混杂；内部门改为白色双扇门；内舱底板和后侧裙板加大，进入后不应再露出火星地表；从居住舱出舱时玩家会落到舱门外更远处，相机保持正常第三人称背后跟随，只临时抬高一点看到人物头部，走出一段距离后恢复默认第三人称，不再切到人物正面。
- 居住舱交互提示改为贴门触发：室外必须贴近居住舱门才显示“进入居住舱”，居住舱主线检查通过进门触发，不再显示远距离“检查居住舱”按钮；室内必须靠近白色内部门才显示“离开居住舱”。维修机器人对话范围收窄为近身距离；移动端普通探索只显示一个最高优先级明确动作，不再出现“多个可互动项”的综合入口。人在居住舱内时只扫描室内出口和照片电子屏，不扫描外部建筑、机器人、车辆、补给点或史蒂夫信息呼叫；史蒂夫等外部信息框在居住舱、温室、飞船内部暂不主动弹出，避免室内画面被外部提醒遮挡。
- 英文版飞行背包提示改为按当前语言直接输出完整句，避免出现中文句子里只把 `E/Q` 操作词替换成 `Ascend/Descend` 的混合提示。
- 金币模型的 `$` 贴面已放大到原视觉约 2 倍，并在金币正反两面各放一个非镜像 `$`。
- 温室模型已改为更完整的可碰撞场景：外部足迹 collider 加密，避免不互动直接穿进透明穹顶；内部移除长条黑色培养槽，替换为多棵不同高度的低多边形树木和灌木，并用局部圆形碰撞限制穿树，只保留一条弯曲通道供亚历克斯通过。
- 中文版可见命名已统一：玩家名显示为“亚历克斯”，基地显示为“阿瑞斯阿尔法基地”，中央 AI 从 Mother 改为“史蒂夫（Steve）”。史蒂夫不是现实人物复刻，而是以乔布斯式“聚焦、端到端体验、质量门禁、直接反馈”为语气框架的机器中控 AI；角色卡已迁移到 `docs/lore/steve.md`。顶部“当前任务”只保留任务目标，不再写 `W/S/A/D`、空格、Shift 等操作教学，操作教学继续走右下角独立提示。
- 场景细节新调：亚历克斯初始职位从“实习巡航员”改为“巡航员”，左侧 HUD 仍只保留文字不加分组边框，人员栏改为职位在上、玩家名在下；史蒂夫信息现在只让顶部 `R` 按钮发红光，按 `R` 后直接进入对话栏，不再弹出接收选项；地表随机金币改为 `3` 组，每组 `10` 枚，每 `2` 分钟统一刷新位置，并避开建筑、物体、球面碰撞体、足球、福福和其它金币组；雷达新增太阳黄色发光点，全屏雷达目标点和标签字更大。
- 任务菜单新调：氧气补给互动只在氧气进入 `20%` 报警区间后出现；氧气未报警时即使路过飞船补给点也不弹“更换氧气背包”，雷达绿色补氧点仍在 `35%` 以下提前显示。巡检车或运输车近距离经过亚历克斯身边时显示“要不要搭便车？”，按 `E` 上车后立即切到第一人称并补满氧气/体能，乘车期间不消耗，上车后可随时按 `Q` 下车。
- 搭便车费用为 `10` 金币，金币不足不能上车；每次成功上车会扣 `10` 金币并奖励 `+10` 积分，可重复获得。两辆可搭乘车辆已退回原 `greatCircleLoop` 大圆轨道，不新增寻点员、站牌或定位实体；只在原轨道上计算离古树门洞正前最近、离基地重生点最近、离坠毁飞船残骸最近的三个停靠角度。两车相位相差半圈，在这些最近点各停留 `30s`，车辆不会直接停在出生点，也不会改变原行车路线。乘车时 `W/S/A/D` 改为上下左右观察，仍可按 `G` 启用相机拍照，也可按 `X` 使用缩放枪；缩放枪瞄准态下 `Q/E` 先用于缩小/放大，退出瞄准后 `Q` 下车。
- 首页底部新增四位数字访客计数器，进入游戏后隐藏，不在游戏内显示统计人数；前端请求 `/api/visitors`，服务器按请求 IP 的 hash 去重后累计人数，统计文件默认保存在 `data/visitor-stats.json`。上线时除了上传 `dist/`，还必须运行 Node 服务并让 Nginx 代理 `/api/` 到该服务；否则首页保持 `0000`。
- 当前交互按键新调：搭便车上车仍为 `E`，下车改为 `Q`；福福低多边形模型已缩小 `50%`，安抚互动范围同步收窄；福福被安抚后自动跟随，首次找到福福奖励 `+40` 积分，并让后续宇航服氧气消耗速度降低 `25%`，已取消按 `Q` 停留/脱离逻辑，不论玩家跑多远、跑多久，福福最终都会继续追上玩家；靠近火星足球按 `E` 拾取，携带时按 `Q` 放下。普通探索状态不再显示世界空间悬浮物体名称，名称仅保留在互动提示、对话和雷达标签中。
- 英文版补齐剧情对白、机器人对白、埃隆 对话、缩放枪提示和建筑互动说明的精确英文映射；`src/main.ts` 新增 `runtimeEnglishTexts`，`scripts/i18n-audit.mjs` 已纳入该表检查。自定义扫描确认英文模式本地化后不再残留中文字符串。
- `public/story-overview.html` 故事概要独立页已补英文翻译；首页点击故事概要时会带上当前语言参数，故事页也会读取 `localStorage["mars.language"]` 兜底。Playwright 已验证 `story-overview.html?lang=en-US` 首屏、正文和章节索引为英文。
- 故事概要页首屏标题已按视口居中；左上角返回主页按钮改为首页同系红色发光交互，不再使用蓝色 hover/focus。
- 首页标题层已改为明确按页面 `50%` 水平居中定位；英文标题使用单独行宽控制，避免切换英文后因为长标题行在宽屏下视觉偏右。移动端竖屏不再把标题块垂直居中，而是固定在页面中上部，和宽屏首页的高度关系保持接近。

## 关键主管文档

- 技术和 UI 规则：`docs/tech-plan.md`
- 重要决策历史：`docs/decision-log.md`
- 世界观、主线和系统总控：`docs/production-blueprint.md`
- 1.0 基础实现交接：`docs/dev-log/v1-implementation.md`

## 下次继续时优先检查

- 首页语言国旗小球的视觉是否满意，尤其是自转速度、明暗面和国旗识别度。
- 首页视角下太阳能阵列是否落在用户标注的亮面区域，是否仍避开温室、03 飞船和基地建筑。
- 移动端对话面板在不同文字长度下，底部按钮是否完全稳定。
- 星链卫星在桌面和移动端天空中是否足够可见但不过分像近处飞行器；缩放枪能否锁定并击中奖励。
- 居住舱实机检查：出舱后玩家是否不再被舱体挡住，且相机仍在人物背后、方向键不反；休眠舱上半部分是否是同形半透明玻璃且没有新增方块，透过玻璃应看到下半实体横截面而不是地面，材质分割线不应出现三角形混杂；电子屏前方是否不再有深色遮挡块；电子屏无照片时的橙红色祝福文字是否清晰且不过界；室内“离开居住舱”是否只在靠近白色门时出现，室外“进入居住舱”是否必须贴近门才出现，史蒂夫等外部信息框不应在室内弹出。
- 近火圆形轨道流星出现时，缩放枪能否锁定这一颗流星本体并触发一次性积分奖励；其它两颗远处天体不应被锁定。
- 对话流程中不应再出现第三选项；雷达金币、任务目标、氧气补给三者应能明显区分。
- 长按 `F` 的全屏雷达是否能保持世界动态刷新但锁住人物操作，松开是否恢复；全屏雷达中 `Q/E` 是否调整雷达范围。
- 体能/氧气新数值需要实际试玩手感：跳跃探索应明显更省体能，但不能让氧气路线规划失效。
- 相机奖励需要实际试玩确认：首次足球穿过远古巨树拱门门洞是否只弹一次三行奖励框；`G` 取景、`E/Enter` 拍照、居住舱电子屏轮播、`90vw` 大图查看和 `E/Enter` 下载是否符合预期；`36` 张上限替换旧照片是否自然。
- 后续新增剧情对白或运行时提示时，优先补 `runtimeEnglishTexts`，再运行 `pnpm i18n:audit` 检查。
- 后续修改故事概要页时，需要同步维护 `public/story-overview.html` 内置英文映射，并用 `?lang=en-US` 做一次真实页面检查。
- 长对话如再次接近 3 次压缩，先检查本文件是否需要更新，再建议开启新对话。
