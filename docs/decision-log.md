# 决策记录

## 2026-06-27

### ARES Starlink-M 火星轨道通信星座

原因：

用户希望参考现实 SpaceX Starlink，为火星基地拟定并实现一套适合火星的星链系统：系统建立后直接生成第一批卫星，之后每 24 个火星日新增 10 颗；同时要求卫星外观和比例贴近现实，并和火星尺度建立真实对比。后续补充要求：缩放枪必须能命中星链卫星模型，并给予积分奖励。

决策：

- 新增 `ARES Starlink-M`，定位为火星低轨通信星座，服务基地内网、轨道中继、风暴备份链路和地火延迟通信排队转发。
- 火星半径按 `3390 km` 映射到当前游戏 `PLANET_RADIUS = 132`；低轨通信层约 `360 km`，游戏高度约 `14.0`；中继层约 `525 km`，游戏高度约 `20.4`。
- 场景初始化时直接生成第一批 `10` 颗卫星，不做释放动画；之后每 `24` 个火星日直接新增 `10` 颗，第一阶段上限 `120` 颗。
- 卫星采用程序化实体模型，保持 Starlink V2 Mini 风格的扁平本体、深色相控阵面板和长太阳翼比例；由于真实比例本体小于 `0.001` 游戏单位，实际渲染采用可见实体代理加微光点。
- 通信塔校准后，雷达和任务提示显示轨道链路状态；星链改善通信稳定性，但不消除地火光速延迟。
- 每颗星链卫星加入缩放枪 raycast 目标池；首次命中单颗卫星时奖励隐藏发现积分，奖励不可重复刷。
- 只有每现实约 3 分钟近火掠过的那颗石质流星本体加入缩放枪 raycast 目标池；命中后按缩放枪倍率变化 60 秒，首次命中奖励隐藏发现积分。其它两颗远处天体不参与缩放枪判断。

已登记文件：

- `src/orbital-starlink.ts`
- `src/world.ts`
- `src/main.ts`
- `src/style.css`
- `docs/tech-plan.md`
- `docs/production-blueprint.md`
- `docs/lore/mars-survival.md`
- `docs/dev-log/current-handoff.md`
- `docs/decision-log.md`

## 2026-06-24

### 线上图片加载速度优化

原因：

用户反馈互联网页面打开时图片加载偏慢，重点包括火星星球贴图、故事概要页场景概念图和角色贴图。排查发现多张 PNG 在 1.4MB 到 2.8MB 之间，线上静态包会直接下载这些大图。

决策：

- 游戏运行时贴图和对话立绘切换为 WebP：火星星球贴图、Alex、Mother、维修机器人、Elon 对话图均改为 WebP 引用。
- 故事概要公开资源 `public/story-assets/` 全部改为 WebP，并删除未引用 PNG 副本，避免部署包继续携带大文件。
- 故事源页面 `docs/story/core-story-v2-visual.html` 同步改用 WebP 资源。
- 项目内概念图也补齐 WebP 版本，文档默认引用压缩图，避免继续把 1MB 以上 PNG 当作预览路径。
- 压缩策略：在尽量保持清晰度的前提下，按实际展示尺寸适度降采样；透明角色图保留 alpha 通道。
- 构建结果中，大图从约 1.4-2.8MB 降到约 55-256KB，火星贴图约 189KB；概念图压缩后约 83-341KB。

已登记文件：

- `src/assets/mars-albedo-generated.webp`
- `assets/portraits/alex-dialogue.webp`
- `assets/portraits/mother-bust.webp`
- `assets/portraits/repair-robot-dialogue.webp`
- `assets/portraits/elon-dialogue-transparent.webp`
- `assets/story/mars-surface-hero.webp`
- `assets/concepts/mars-engineer-character.webp`
- `assets/concepts/mars-rover-robots.webp`
- `assets/concepts/mother-ai-terminal.webp`
- `assets/concepts/elon-character-concept.webp`
- `assets/concepts/mars-base-layout.webp`
- `assets/concepts/fufu/fufu-astronaut-concept.webp`
- `assets/concepts/fufu/fufu-spritesheet.webp`
- `assets/portraits/fufu-dialogue.webp`
- `public/story-assets/*.webp`
- `src/world.ts`
- `src/dialogue/dialogues.ts`
- `public/story-overview.html`
- `docs/story/core-story-v2-visual.html`
- `docs/art-direction.md`
- `docs/production-blueprint.md`

### 黑色石碑赠送放大缩小枪概念

原因：

用户希望把远端黑色石碑从单纯异常点扩展为可互动彩蛋：玩家在石碑处对话后获得一把“放大缩小枪”，后续可瞄准物体并放大或缩小。

决策：

- 当前场景已存在黑色石碑，位置记录在 `src/world.ts` 的 `world.monolith`，此前仅作为远端异常点和声音触发点。
- 黑色石碑改名为“黑色方碑”，形状改成更宽的直立矩形，去掉底座，仅保留暗色接触阴影。
- 方碑位置从 `(260, -315)` 移到 `(-360, -300)`；按现有建筑/飞船/太阳能阵列表面弧长计算，最近距离从约 `19` 提升到约 `32`，不再靠近医疗舱。
- 放大缩小枪第一阶段先作为概念资产入库，并制作程序化低多边形手持模型；模型默认隐藏，必须通过黑色方碑互动获得后才装备到 Alex 右手。
- 方碑获取逻辑：靠近出现 `E` 互动提示；按 `E` 打开对话界面，右侧对话对象显示 45 度透明背景黑色方碑立绘，缩放枪图片只出现在对话框内容区；按钮流程为先点“获取”获得工具，再点“继续”退出对话。
- 玩法采用“装备态 + 瞄准态 + 确认操作”三段式：按 `U` 装备/进入瞄准，屏幕中央出现缩放枪准星，锁定目标后按 `=` 放大、按 `-` 缩小。
- 缩放对象先尽量开放，但不缩放火星星球本身和天上的星星本身；其他星球上的物体、场景内物体都可以被缩放。
- 缩放效果有时间限制：每次缩放持续 1 分钟，时间结束后自动恢复到原始尺寸。
- 数值限制仍按相对原始尺寸计算，放大和缩小极限都是 3 倍：最大 `3x`，最小约 `0.33x`。
- 新增概念图 `assets/concepts/scale-gun-concept.png`，深色底、简洁流线型、适合宇航员手持。
- 新增黑色方碑绿幕源图 `assets/portraits/monolith-dialogue-green.png`，仅作为抠图源；运行时使用透明版 `assets/portraits/monolith-dialogue-transparent.webp` 作为方碑对话的右侧角色立绘。
- 方碑对话文案只做玩家可读的缩放枪介绍，不展示设计参数、效果时长、数值上限或“头盔自动展开图纸”等实现解释。
- 缩放枪 3D 模型已使用 Three.js 几何体拼装，材质沿用宇航服暖白、石墨黑、橙色安全件和蓝色镜头。

### 2026-06-25 可试玩修复

- 缩放枪从“只获得模型”补为可用玩法：`U` 开关瞄准态，`=` 放大，`-` 缩小，准星显示当前锁定目标，缩放效果 60 秒后恢复。
- 03 飞船上的 Elon 模型保持机器狗头，但身体改为更接近正常人形宇航服比例，不再使用横向大肚子躯干。
- 火星车和运输车最终位置增加建筑碰撞推出，避免路线偏转不足时穿过建筑。
- 3 艘飞船的维修机器人巡逻点外移到飞船底部范围之外。
- 小屏触摸互动菜单改为 `pointerdown` 立即触发，并缓存菜单渲染签名，避免按钮每帧重建导致触摸 `click` 丢失。
- 对话舞台统一改为“单说话者模式”：所有端都保留原本左右站位，只显示当前说话者头像和对应侧姓名牌，隐藏听话方头像和姓名牌；说话者不再额外放大。小屏/触摸端继续隐藏左右姓名牌，只保留对话框顶部的当前说话者信息。
- 黑色方碑赠送缩放枪的对白不再展示缩放枪物品图，桌面和移动端都只保留文字说明与获取按钮，减少对话框高度跳动。
- 对话框改为固定舞台参数：面板使用稳定高度和固定底部位置，正文区域内部滚动，按钮保留在底部；角色头像统一压在面板后方，只露出约半身，避免不同角色忽高忽低、忽大忽小。
- 飞船维护机器人改为按飞船所在小星球径向向外布置巡逻点，不再使用同一组固定飞船本地偏移，避免 02 货运飞船维护工被推到飞船底部阴影内。

已登记文件：

- `assets/concepts/scale-gun-concept.png`
- `assets/concepts/scale-gun-concept.webp`
- `assets/portraits/monolith-dialogue-green.png`
- `assets/portraits/monolith-dialogue-green.webp`
- `assets/portraits/monolith-dialogue-transparent.png`
- `assets/portraits/monolith-dialogue-transparent.webp`
- `src/player.ts`
- `src/world.ts`
- `src/main.ts`
- `src/dialogue/dialogues.ts`
- `index.html`
- `src/style.css`
- `docs/decision-log.md`

### HUYEE 神秘代码与飞行模式

原因：

用户希望新增一个隐藏作弊码：不论当前处于什么界面，只要输入 `HUYEE` 就播放“叮咚”音，并切换角色飞行能力。再次输入同一代码后关闭飞行，回归普通移动和跳跃。

决策：

- 全局监听连续字母输入 `HUYEE`，触发后播放两段式“叮咚”合成音。
- 飞行模式只改变室外星球表面移动：按住空格持续上升，按住 `Control/Ctrl` 持续下降，WASD/方向键继续控制前后和转向。
- 进入居住舱、温室、飞船内视角或升降梯时，不启用飞行移动，避免破坏室内和任务交互边界。
- 飞行开启时，Alex 背包两根圆柱状罐体下方显示两团蓝色喷焰；喷焰使用程序化 ConeGeometry 和加色材质，不新增图片资源。
- 该功能定位为隐藏彩蛋/调试能力，不写入普通操作指南主流程。

已登记文件：

- `src/main.ts`
- `src/player.ts`
- `docs/tech-plan.md`

### 火星车轮迹与细雾粉尘

原因：

用户希望火星车驶过厚尘地表时留下车轮压痕，并在车尾生成细腻雾气型粉尘，要求不能做成大颗粒飞沙，且线上网站也要明显看得出效果。

决策：

- 第一版采用轻量视觉方案：贴地半透明轮迹 mesh + 低透明度软边缘 Sprite 粉尘。
- 轮迹由有限对象池循环复用，沿车辆两侧轮位生成，贴合弧面火星地表。
- 粉尘只在车尾和后轮附近生成，颜色接近火星尘土，透明度低，会扩散、变淡、消失，避免大颗粒视觉。
- `02 车辆 运输车` 调整为机器人车库周边低速巡逻，保证玩家进入基地附近就能看到轮迹和车尾粉尘。
- 暂不做实时地形变形或动态地表纹理，因为当前项目目标是网页端稳定运行，第一版应优先保证桌面和移动端性能。

已登记文件：

- `src/world.ts`
- `docs/tech-plan.md`

### 玩家背景页视觉优化

原因：

用户希望用 `frontend-design` 优化 `docs/story/core-story-v2-visual.html`，让该页面更像玩家可读的游戏背景介绍，而不是普通文档页。随后用户进一步要求：封面居中、不拉大字距；删除“创作基准”；内容改成更像真实档案的故事页；福福角色卡使用和其他角色一致的深色背景。

决策：

- 页面定位为“火星任务档案”风格：首屏使用基地概念图、任务状态条和大标题建立沉浸感。
- 封面标题居中排列，不再拆字拉开字间距。
- 保持玩家公开信息边界：不展示隐藏角色 Elon，不恢复“场景与资产”概念设定区。
- 删除“创作基准”区和“玩家/创作者/游戏背景页”等出戏表达，整体改成 ARES BASE ALPHA 公开档案口吻。
- 色彩基于火星尘土、薄壁不锈钢、氧气 HUD 蓝和温室绿，避免模板化单色科幻风。
- 核心角色卡增加身份标签，章节区改为任务记录式卡片，正文区强化阅读节奏和章节索引。
- 新增 `assets/portraits/fufu-dialogue.png`，用于福福角色卡深色立绘展示；原 `assets/concepts/fufu/fufu-astronaut-concept.png` 继续作为飞船残骸场景概念图。
- 章节索引增加滚动同步高亮：阅读到对应正文段落时，右侧导航自动标记当前章节。
- 首屏背景从基地鸟瞰图改为更真实的火星地表图，底部渐隐到黑；基地鸟瞰图移到首屏之后，作为完整满宽图片展示，说明文字放在图片下方。
- 移动端需要保持标题、状态条、角色卡和正文无横向溢出。

已登记文件：

- `docs/story/core-story-v2-visual.html`
- `public/story-overview.html`
- `public/story-assets/fufu-dialogue.png`
- `assets/story/mars-surface-hero.png`
- `public/story-assets/mars-surface-hero.png`
- `assets/portraits/fufu-dialogue.png`
- `docs/lore/fufu.md`
- `docs/art-direction.md`


### 福福宇航服概念图

原因：

用户明确福福在火星室外/太空环境中应戴宇航员头盔并穿宇航服，而不是普通猫形象。该图用于玩家可见的游戏背景与角色介绍网页。

决策：

- 新增 `assets/concepts/fufu/fufu-astronaut-concept.png`。
- 福福保持黑白奶牛猫、黄色眼睛、白色脸纹、鼻子旁边明显小黑点/黑斑、黑色下巴等核心识别点。
- 火星室外形象增加小型透明头盔、轻量宠物 EVA 服、生命维持背包和状态灯。
- 福福的猫耳朵也必须是宇航服头盔/耳罩结构的一部分，腿和脚必须有 EVA 护腿、靴套或爪部护具，不能裸露在火星室外环境中。
- 最终概念图背景改为参考 SpaceX Starship 气质的薄壁不锈钢飞船遗骸角落，避免厚重装甲、干净基地、普通杂物或湿漉漉粘稠破损物，更贴合“福福在事故残骸旁被发现”的剧情。
- `docs/story/core-story-v2-visual.html` 中的福福展示图改用宇航服概念图。
- 旧 spritesheet 仍保留作为游戏动作和造型参考。

已登记文件：

- `assets/concepts/fufu/fufu-astronaut-concept.png`
- `docs/story/core-story-v2-visual.html`
- `docs/lore/fufu.md`
- `docs/art-direction.md`

### 游戏背景与角色介绍可视网页

原因：

用户希望把文章《火星先遣队：第一位人类居民》做成可视网页。该网页的作用是介绍游戏背景以及相关角色，并附带场景和角色概念图；不修改游戏本体名称。

决策：

- 新增静态网页 `docs/story/core-story-v2-visual.html`。
- 页面引用现有项目素材：基地布局、Alex、Mother、机器人、福福等概念图和对话图；Elon 是隐藏角色，不出现在玩家公开介绍页。
- 页面结构包括：封面、游戏背景、核心角色、章节脉络、场景资产、故事正文摘编、创作基准。
- 该页面为文档展示页，不接入游戏主流程，不改变现有 Vite 游戏入口，也不修改游戏名称。

已登记文件：

- `docs/story/core-story-v2-visual.html`
- `docs/story/core-story-v2-rational-wonder.md`
- `docs/production-blueprint.md`

### 新增 Elon 实验性 AI 角色设定

原因：

用户希望创建一个名为 Elon 的新角色，用来测试未来“固定知识库 + 开放式 AI 问答”的对话能力。该角色需要借鉴本地 `elon-musk-perspective` 技能中的工程判断框架，但不能在本次直接进入游戏场景、模型或代码。

决策：

- 新增 `docs/lore/elon.md`，作为 Elon 的角色卡。
- 新增 `docs/ai/elon-agent-design.md`，作为未来 AI 对话接口、prompt、知识库和边界规则的设计草案。
- Elon 定位为 ARES 计划构建的实验性工程顾问 AI，不是现实中的 Elon Musk 本人，也不是数字复活。
- Elon 的思维框架采用第一性原理、白痴指数、五步算法、垂直整合、快速迭代和多行星文明尺度。
- Elon 和 Mother 不构成敌对关系：Mother 负责安全、保护和长期稳定；Elon 负责挑战默认假设；Alex 保留现场决策权。
- 当前只创造设定和 AI 对话设计，暂不创建游戏内人物模型、场景实体或代码实现。

已登记文件：

- `docs/lore/elon.md`
- `docs/ai/elon-agent-design.md`
- `docs/production-blueprint.md`

### GitHub 发布同步入口

原因：

项目正在由多个 Codex 对话并行修改，需要一个固定入口检查未提交、未上传和未合并的修改，最终形成可部署版本。

决策：

- GitHub 仓库保持公开，方便部署服务拉取和更新。
- 这个对话作为发布同步入口。
- 每次发布前检查 `git status`、本地分支、远端分支和 GitHub 仓库可见性。
- 如有未提交改动，先审查、构建验证，再提交。
- 如有未合并分支，确认后合并到 `main`。
- 最终推送到 GitHub `main`，供后续部署网站使用。
- 仓库公开后可用于常见静态部署平台；后续如需分支保护，再单独配置 GitHub ruleset 或 branch protection。

### 新增核心故事母本

原因：

项目需要一个能统一主线任务、支线任务、角色对白、结局气质和美术氛围的长篇故事基础，而不是只依靠零散任务说明。

决策：

- 新增 `docs/story/core-story-v1.md`。
- 故事采用小说式写法，融合当前所有核心元素：
  - Alex 抵达火星。
  - Mother 与人类现场判断权。
  - 居住舱、氧气站、太阳能阵列、机器人车库、物资仓、温室、科研舱、医疗舱、通信塔、甲烷燃料厂。
  - A-12、A-01、P-03 三类机器人。
  - 福福支线。
  - 通信延迟与火星风暴。
- 基调确定为硬核科幻但温和轻松，没有反派，冲突来自对错、原则和非原则的边界，最终落到人性共鸣。

已登记文件：

- `docs/story/core-story-v1.md`
- `docs/production-blueprint.md`

### Elon支线任务与固定对白池

原因：

用户进一步明确：Elon当前虽然是实验型开放角色，但第一阶段仍要依靠固定知识库和大量固定对白互动。玩家需要在 `03 飞船 返回飞船` 上找到它，但 3 号飞船升降梯初始无法运行，必须先完成修复任务。

决策：

- Elon 在游戏内本名就是 `Elon`，只是同名，不指向现实中的 Elon Musk 本人，也不是数字复活。
- Elon 可被理解为接近马斯克式思维的工程思想人格或“精神体式”顾问，但不代表现实人物发言。
- Elon 的位置设在 `03 飞船 返回飞船` 升降平台到达顶端后的固定高空廊道，靠近飞船舱门处。
- `03 飞船 返回飞船` 内舱不可进入，玩家只在外部高空廊道靠近舱门处与 Elon 互动。
- 新增解锁支线：玩家需要寻找执行器驱动轴、高功率继电器、姿态锁止传感器、低温润滑胶囊，并在科研舱生成校准密钥，修复升降梯后才能见到 Elon。
- 升降梯修好后，玩家后续可以随时乘坐 3 号飞船升降梯，到达上层高空廊道与 Elon 交流。

后续修正：

- 用户明确要求 Elon 不在飞船内部，也不是站在升降平台本身上，而是在升降平台到达顶端后的高空廊道，靠近飞船舱门处。
- `03 飞船 返回飞船` 内舱对所有人不可进入；当前实现中靠近舱门时触发 Elon 对话，不进入飞船内舱。
- 新增大型固定对白池草案。每组对白有 ID，后续实现时应优先播放未读内容，避免玩家每次聊天都重复。
- 该阶段先写脚本和设计文档；后续已接入游戏代码和人物模型，见下方最新记录。

已登记文件：

- `docs/tasks/elon-ship-03-elevator-sidequest.md`
- `docs/dialogue/elon-fixed-dialogue-draft-v1.md`
- `docs/lore/elon.md`
- `docs/ai/elon-agent-design.md`
- `docs/production-blueprint.md`

### Elon 角色形象资产入库

原因：

用户确认 Elon 的角色形象方向：站立的人形机器狗，身体是人形宇航服结构，头部像机器狗。该图需要用于后续游戏对话区制作。

决策：

- 确认 Elon 第一版形象为人形机器狗，不使用真人脸，不指向现实人物。
- 保存完整概念图到 `assets/concepts/elon-character-concept.png`。
- 保存对话区引用图到 `assets/portraits/elon-dialogue.png`。
- 删除重复临时副本，只保留上述两张项目资产。
- `src/dialogue/dialogues.ts` 中的 Elon 对话角色已从临时 SVG 占位图切换为真实对话图。

已登记文件：

- `assets/concepts/elon-character-concept.png`
- `assets/portraits/elon-dialogue.png`
- `assets/portraits/elon-dialogue-transparent.png`
- `src/dialogue/dialogues.ts`
- `docs/lore/elon.md`
- `docs/art-direction.md`

补充：

- 新增 `assets/portraits/elon-dialogue-green.png`，作为绿色背景版本，方便后续抠图或制作透明对话立绘。
- 当前游戏已从绿幕源图生成透明对话图 `assets/portraits/elon-dialogue-transparent.png`；绿色残边已压为深灰，运行时使用透明版。

### Elon 游戏内模型与高空廊道站位接入

原因：

用户要求 Elon 真正出现在当前游戏中，并强调位置不是飞船内部，也不是升降平台本身，而是升降平台到达顶端后的高空廊道，靠近飞船舱门处；模型需要参考完整身体设定，约 2 米高，略高于 Alex，并看向火星基地。

决策：

- 03 飞船升降梯修好后，玩家可上行到固定高空廊道，与 Elon 对话。
- Elon 的 3D 模型使用简洁几何体重建：机器狗头部、人形宇航服身体、浅色装甲、深色关节、橙色点缀和蓝色发光传感器。
- Elon 挂在升降梯固定结构上，不挂在移动升降平台车厢上；站位靠近飞船舱门侧，身体朝向基地方向。
- 03 飞船内舱仍不可进入；到达舱门位置时触发 Elon 对话，而不是进入飞船。
- 对话角色图使用由绿幕源图处理出的 `assets/portraits/elon-dialogue-transparent.png`。

已登记文件：

- `src/world.ts`
- `src/main.ts`
- `src/dialogue/dialogues.ts`
- `docs/lore/elon.md`
- `docs/tasks/elon-ship-03-elevator-sidequest.md`

### 飞船内舱视角与几何体简化

原因：

用户反馈飞船内舱只能向前和向下看，无法抬头看到飞船内部顶端；同时内舱里有几块额外几何体影响观察。

决策：

- 飞船内舱移除控制台、侧边箱体、后部封板等额外几何体。
- 保留舱体圆筒、结构肋骨、地板和舱顶灯条，用更清爽的结构表达飞船内部。
- 飞船内第一人称视角放宽向上 pitch 限制，让玩家可以抬头看到舱顶。

已登记文件：

- `src/world.ts`
- `src/main.ts`

### 建筑匹配机器人外移到外围巡逻

原因：

用户发现部分建筑匹配机器人会待在建筑物内部，导致玩家无法靠近通讯。机器人应避开自己的建筑体，在建筑外围走动或站立。

决策：

- 重新检查所有建筑、飞船和太阳能阵列对应的维修机器人。
- 将温室维修工、太阳能阵列 A/B/C 维修工、飞船维护工等高风险点外移到建筑碰撞范围外。
- 修改维修机器人巡逻点生成方式：以建筑外侧方向为基准，沿外围切向移动，并只向外扩展，不再生成朝建筑内部穿行的巡逻点。
- 保留现有固定碰撞推出逻辑，并额外用几何检查验证巡逻点和巡逻线段都避开自身建筑碰撞范围。

已登记文件：

- `src/world.ts`

### 远端暗面黑色石碑异常点

原因：

用户希望在星球绝对暗面、建筑物非常稀少的位置放置一个黑色石碑，气质可参考经典科幻中的神秘石碑；玩家靠近时会听到滴滴声。

决策：

- 新增原创黑色石碑异常点，不直接复刻具体电影画面，只保留“远端、黑色、极简、沉默”的科幻气质。
- 石碑放在初始太阳方向的背光远端，远离主基地建筑群和常规巡检路线。
- 石碑模型使用低多边形几何体：黑色高直立长方体、细暗边线、低矮基座和暗色接触阴影。
- 石碑不是任务交互物；玩家靠近一定范围后自动触发双短音“滴滴”。
- 滴滴声用 Web Audio 程序合成，不新增音频文件；距离越近，滴声间隔越短、音量越明显。

已登记文件：

- `src/world.ts`
- `src/main.ts`
- `docs/art-direction.md`

### 首页故事概要占位按钮

原因：

用户希望在首页“进入基地”按钮下方新增“故事概要”按钮，并接到“游戏编剧”创作的故事概要网站。

决策：

- 首页标题区新增“故事概要”按钮，位于“进入基地”下方。
- “故事概要”点击后先播放一个短促“滴”声，再跳转到 `/story-overview.html`。
- “进入基地”开始进入游戏时也播放同一类短促“滴”声，作为按钮转换反馈。
- 首页两个按钮默认都使用深色描边样式，只有鼠标悬停时切换为橙色填充样式。
- 首页两个按钮使用正常长方形，不再保留四角切角造型。
- 标题页默认选中“进入基地”；支持键盘上下键或鼠标悬停切换当前按钮，切换时播放短促“滴”声，按 Enter 或 Space 执行当前选中项。
- 滴声使用 Web Audio 程序合成，不新增音频文件。
- 故事概要网页由 `docs/story/core-story-v2-visual.html` 发布副本生成，运行时文件放在 `public/story-overview.html`。
- 故事页依赖图片复制到 `public/story-assets/`，确保构建部署后仍可访问。

已登记文件：

- `index.html`
- `src/main.ts`
- `src/style.css`
- `public/story-overview.html`
- `public/story-assets/`

### 游戏内退出确认窗口

原因：

用户反馈 `Esc` 一键退出太快，容易误触导致当前巡检进度丢失，需要在返回主页前增加确认。

决策：

- 游戏中按 `Esc` 不再直接返回主页，而是打开退出确认窗口。
- 确认窗口提供“返回游戏”和“退出游戏”两个按钮，默认选中“返回游戏”。
- 键盘左右键或上下键可切换选项，按 Enter 或 Space 执行当前选中项。
- 只有选择“退出游戏”并确认后才调用返回主页逻辑。
- 打开确认窗口时清空当前移动输入，避免角色继续移动。

已登记文件：

- `index.html`
- `src/main.ts`
- `src/style.css`
- `docs/tech-plan.md`

### 核心故事向理性惊奇方向优化，并新增机器人对白草案

原因：

用户希望故事背景更接近阿瑟·克拉克式的硬科幻气质：宏观、理性、带有宇宙尺度的惊奇；同时当前游戏内机器人对白过短，需要扩展为 2-3 轮对话，但暂时不接入代码。

决策：

- 新增 `docs/story/core-story-v2-rational-wonder.md`，作为核心故事背景 v2。
- v2 不直接模仿具体文本，而是借鉴“理性惊奇”方向：更克制、更宏观、更强调技术系统和宇宙尺度。
- 新增 `docs/dialogue/robot-dialogue-draft-v1.md`，覆盖 15 个现有设施/飞船/阵列/任务机器人。
- 每个机器人对白保持执行单元气质，不写成复杂人格，但扩展为 2-3 轮，补充设施功能、当前风险和火星工程逻辑。
- 本次只写文档草案，不修改游戏代码。

已登记文件：

- `docs/story/core-story-v2-rational-wonder.md`
- `docs/dialogue/robot-dialogue-draft-v1.md`
- `docs/production-blueprint.md`

### 将核心故事 v2 和机器人对白草案接入当前游戏

原因：

用户希望把“游戏编剧”对话中已经形成的最新故事背景和机器人对白，直接应用到当前游戏，而不是继续停留在文档草案。

决策：

- 保留 `docs/story/core-story-v2-rational-wonder.md` 作为当前剧情气质基准。
- 将 `docs/dialogue/robot-dialogue-draft-v1.md` 中 15 组机器人对白接入 `src/dialogue/dialogues.ts`。
- 运行时根据机器人 `userData.label` 匹配对应对白：3 艘飞船维护工、9 个建筑维修工、3 个太阳能阵列维修工。
- 对话系统新增普通 `next` 推进能力，让非选项对白可以自然进入 2-3 轮。
- 福福相关提示改为“隔离、扫描、确认安全、进入观察名单”的工程化善意逻辑。

实现边界：

- 本次不新增 3D 模型、不接入 AI 后端、不处理仍在进行中的 Elon 角色设定。
- A-12、A-01、P-03 的专属任务对白仍主要通过主线/支线提示呈现，后续可再升级为独立可交互对象。

### 任务扩展采用 3 条主线任务包 + 3 条支线任务包

原因：

当前 1.0 主线只覆盖开场、氧气异常、太阳能阵列 C 和机器人车库，几分钟即可完成，适合试玩引导，但不足以承载项目主题。

决策：

- 新增 `docs/tasks/mission-expansion-v2.md`，作为下一阶段任务设计依据。
- 主线扩展为：
  - 生命支持验收。
  - 温室第一粒种子。
  - 通信延迟与风暴协议。
- 支线扩展为：
  - 未登记生命体。
  - A-01 的错位货箱。
  - 巡逻线之外。
- 所有任务继续围绕氧气、能源、温室、物资、通信、机器人调度、Mother 信任和 Alex 现场判断权展开。
- 不引入外星人、战斗或玄幻危机。

已登记文件：

- `docs/production-blueprint.md`
- `docs/tasks/mission-expansion-v2.md`

## 2026-06-23

### 1.0 部署方案采用静态站点

原因：

当前版本是 Vite + TypeScript + Three.js 的纯前端网页游戏，没有后端 API、数据库或服务端渲染需求。

决策：

- 1.0 上线以静态部署为主。
- 本地或服务器执行构建，产物为 `dist/`。
- 服务器优先使用 Nginx 托管 `dist/`。
- 后续接入 DeepSeek 对话时，再新增后端 `/api/dialogue`，API Key 不进入前端。

已登记文件：

- `docs/deployment.md`
- `docs/tech-plan.md`

### 背景音乐改为原曲压缩版

原因：

原背景音乐 MP3 约 10MB，占当前部署包主要体积。Web Audio 合成器虽然体积小，但听感与原曲不一致。

决策：

- 保留原曲听感，使用 64kbps MP3 压缩版。
- 前端导入 `src/assets/audio/mars-background-light.mp3`。
- 原 10MB MP3 保持在 `.gitignore` 中，避免误提交到 GitHub。

### 项目采用文件化共同记忆

原因：

长对话会变慢，不同 Codex 对话不会自动共享全部上下文。

决策：

- 聊天只作为工作过程。
- 项目文件作为共同记忆。
- 新对话必须先读 `AGENTS.md`、`docs/project-management.md`、`docs/production-blueprint.md`。
- 重要创意、修改和实现结果必须写回项目文件。

### 福福进入项目设定

来源：

另一个对话已把“宠物福福”写入当前项目文件。

已登记文件：

- `docs/lore/fufu.md`
- `assets/concepts/fufu/fufu-spritesheet.png`

当前定位：

福福是黑白奶牛猫，是火星基地里的情感陪伴角色，不是战斗单位。第一阶段建议作为坠毁飞船残骸旁可救助、可跟随的小猫实现。

### 1.0 可试玩版本交接记录

决策：

- 新增 `docs/dev-log/v1-implementation.md` 作为 1.0 当前状态的快速交接文件。
- 新对话继续开发前，除 `AGENTS.md`、`docs/project-management.md`、`docs/production-blueprint.md` 外，也应阅读该交接记录。

当前状态摘要：

- 已具备火星基地探索、主线任务、对话、地图、氧气背包、移动端控制、01/02 飞船升降梯和封面视觉。
- 桌面端操作规则定为：`E` 进入互动；方向键选择；`Enter` 确认。
- 触摸端操作规则定为：左侧虚拟摇杆移动，右下角大按钮为“跳”，大按钮左侧小按钮为“加速”；靠近互动对象时先显示小型“互动”入口，点开后用底部抽屉选择具体互动项；对话选项和“继续”都可直接点。
- HUD 中“自养量”已统一改为“制氧量”。

## 2026-06-25

### 移动端对话改为纯对话模态

原因：

手机和触摸屏空间有限，原方案让对话面板、角色头像、雷达、任务提示和移动按钮互相挤压，容易出现角色脸部被遮挡、互动按钮不清晰的问题。

决策：

- 移动端和触摸屏进入对话时，隐藏 HUD、任务条、雷达、标签、摇杆、移动按钮和缩放枪瞄准界面。
- 对话保留全屏模糊背景，只展示当前说话角色、对话文本、选项或“继续”按钮。
- 角色仍按原来的左右站位显示；谁说话就显示谁，不说话的一方隐藏，不再额外放大。
- 当前任务从雷达面板中移出，独立显示在游戏界面顶端居中。
- 操作指南暂时从界面中隐藏。

### 触摸端互动菜单改为底部抽屉

原因：

小屏幕上 2 个或以上互动按钮会和摇杆、跳跃、加速按钮重叠，直接展开所有互动项会让点击目标不清晰。

决策：

- 触摸端靠近互动对象时，先显示一个固定在操作区上方的小型“互动”入口。
- 点击“互动”后进入临时选择状态，底部抽屉纵向列出全部互动项。
- 抽屉打开时，摇杆和移动端动作按钮压暗并停止接收事件。
- 用户点互动项后执行操作；点“收起”或抽屉空白区域可回到游戏操作状态。

### 两辆车辆改为同速大环线巡航

原因：

原运输车在机器人车库附近做小半径巡逻，建筑碰撞推出逻辑会让它靠近建筑后侧身慢速漂移；两辆车速度也不一致。

决策：

- `01 车辆 电动巡检车` 和 `02 车辆 运输车` 使用同一个巡航速度。
- 取消运输车的车库小环线，两辆车都改为沿星球 great-circle 大环线巡航。
- 两辆车只通过路线 offset 拉开距离，保持同速同向。
- 大环线方位选择避开基地密集建筑，建筑碰撞保留为兜底保护，不再作为常态行驶方式。

### 桌面端对话框降低高度并抬高角色露出

原因：

桌面网页模式下原对话框高度过大，文本和按钮之外留下明显空白；同时角色头像被面板遮住，只露出头顶，削弱对话表现。

决策：

- 桌面端对话框高度降低到约 168-196px 区间，接近“文本加按钮三行”的紧凑高度。
- 桌面端角色头像向上抬高，保证说话角色露出上半身。
- 移动端全屏对话规则保持不变。

### 飞船维护机器人移出飞船底座投影区

原因：

02 号飞船维护机器人虽然已在碰撞体外，但仍位于货运飞船底座/船体投影下方，玩家只能看到腿部，且难以接近交互。

决策：

- 飞船维护机器人不再只按飞船中心径向外移。
- 新位置采用“径向外移 + 切向侧移”，把机器人放到飞船侧前方安全区，避开底座和船体投影。
- 所有维修机器人安全点外推余量从建筑碰撞半径外 `1.15` 提升到 `2.4`，运行时判定阈值同步提高，降低视觉上贴近建筑的风险。
- 已用几何审计检查 15 个维修机器人，未发现仍被困在固定碰撞体内的机器人。

### 桌面端双选项改为 Q/E 单手操作

原因：

大屏幕同时出现两个互动或对话选项时，依赖鼠标或 `Enter` 会打断左手移动区操作；玩家希望能连续按 `E` 推进对白，并在二选一时仍保持单手完成。

决策：

- 桌面端无选项对白使用 `E` 推进，保留 `Enter` 兼容。
- 桌面端两个并列选项时，`Q` 直接执行左侧，`E` 直接执行右侧。
- 多于两个选项时，保留方向键切换，并允许 `E` 或 `Enter` 确认当前选中项。
- 触摸端点击规则不变。

### 雷达未知目标默认显示为问号

原因：

项目早期雷达机制已经把未知生物或未知物体用 `?` 表示；后来加入的 `O` 键未知点开关让玩家需要额外记忆，而且 Elon 和黑色方碑这类未知对象不应该直接在雷达上暴露名称。

决策：

- 取消 `O` 键显示未知点的功能。
- 游戏开始后，雷达直接同时显示已知设施和未知对象。
- 未知生物、未知残骸、黑色方碑和 Elon 在雷达上统一显示为 `?`。
- Elon 作为未知智能体加入雷达未知对象列表，不新增世界悬浮标签。

### 桌面端常用键位向左手集中

原因：

玩家希望减少右手离开鼠标或左手离开 WASD 区域的次数。原头盔灯 `I` 和地图 `M` 都偏右，不适合单手连续操作；第一人称此前只能靠滚轮逐步缩近。

决策：

- 头盔灯从 `I` 改为 `R`。
- 地图从 `M` 改为 `F`，长按 `F` 进入全屏地图，右上角地图按钮同步显示 `F`。
- 新增 `C` 一键进入第一人称，相当于把相机直接缩到宇航员头部位置；退出第一人称仍可通过鼠标滚轮拉远。

### 缩放枪键位改为 X 与 Q/E

原因：

缩放枪原来用 `U` 开关瞄准、`=` 和 `-` 调整大小，键位偏右，不符合当前“左手集中操作”的方向。玩家希望瞄准后继续使用 `Q/E` 完成缩小和放大。

决策：

- 缩放枪开关瞄准从 `U` 改为 `X`，`U` 不再绑定缩放枪功能。
- 缩放枪瞄准态下，`E` 放大，`Q` 缩小。
- `=` 和 `-` 的缩放设定保留，作为备用操作。
- 瞄准态下 `Q/E` 优先控制缩放枪，不触发普通互动或二选项操作。

### 低氧时雷达高亮补氧点

原因：

氧气压力是当前 1.0 的核心生存反馈之一，但玩家低氧时只看到数值和文字警告，不一定知道最近的补氧点在哪里。任务目标已经有绿色放大点，补氧目标应该使用同一套雷达语言。

决策：

- 氧气背包低于或等于 35% 时，雷达开始高亮可补氧地点。
- 居住舱补给柜、氧气生产站和登陆飞船补给舱显示为放大的橙色发光点。
- 橙色补氧点的尺寸、发光和排序逻辑参考绿色任务目标，但任务目标仍保持更高优先级。
- 氧气高于 35% 时，补氧点不额外高亮，避免常态雷达过度拥挤。

### 体能改为居住舱恢复型生存指标

原因：

体能原来主要跟加速短时消耗和自动恢复绑定，玩家很难感知它是一个需要管理的生存指标。当前希望体能一般不容易耗完，但持续加速、连续跳跃会带来明显成本，并让居住舱承担“休整点”的作用。

决策：

- 地表普通移动会缓慢消耗体能。
- 持续加速和每次跳跃都会增加体能消耗。
- 体能低于或等于 20% 时提示回居住舱休整。
- 氧气补给点只补氧，不补体能。
- 进入或离开居住舱会补满氧气和体能。
- 体能耗尽时，Alex 在居住舱内重新同步，氧气和体能恢复满值。

### 出室内后临时使用人物正面镜头

原因：

从居住舱或飞船内舱出来时，普通跟随镜头会落在建筑或飞船主体一侧，导致建筑遮挡宇航员身体，玩家短时间看不清角色位置和移动方向。

决策：

- 从居住舱、温室或飞船内舱出来时，启动临时正面镜头。
- 临时镜头站在人物正前方看向 Alex，让主体建筑位于角色背后，不遮挡角色身体。
- 玩家离出口达到安全距离后，自动恢复普通第三人称跟随视角。
- 进入室内、重生或回标题时清除临时镜头状态。

### 09 医疗舱机器人外移到门外

原因：

09 机器人原来贴近医疗舱侧前方，容易被医疗舱模型或碰撞修正推到不易看见的位置，玩家到医疗舱时会误以为机器人被困在舱内。

决策：

- 09 机器人改到医疗舱正门外更开阔的巡逻点。
- 医疗舱模型可见编号从 06 统一改为 09。
- 医疗舱碰撞标签同步改为 09，避免后续维护时继续混淆编号。

### 视角复位和缩放枪倍率调整

原因：

`C` 只进入第一人称会让玩家不知道如何快速切回第三人称；`=` 和 `-` 调整镜头后也需要一个明确的默认视角复位键。缩放枪原来的 3 倍上限偏保守，玩家希望效果更明显。

决策：

- 缩放枪缩放范围从 `0.33x` 到 `3x` 调整为 `0.25x` 到 `4x`。
- `C` 改为第一人称和默认第三人称切换。
- 新增 `0` 键，一键恢复默认第三人称视角。
- 缩放枪瞄准态仍只用 `X` 循环打开/收起；`C` 和 `0` 不负责收起缩放枪。

### 体能加速跑消耗对齐跳跃

原因：

体能需要让玩家直觉理解不同动作的成本。跳跃是一次性扣除，加速跑是持续消耗，两者需要有明确换算关系。

决策：

- 一次跳跃消耗 `4` 点体能。
- 加速跑每秒消耗等于一次跳跃消耗，即每秒 `4` 点体能。
- 普通移动仍保持低消耗，避免玩家正常探索时频繁耗尽体能。

## 2026-06-25：多人 v1 采用免注册公共大厅

原因：

用户希望不同人进入游戏时自动生成新的宇航员，彼此可见，任务相同且不需要注册。当前 1.0 是纯前端静态网页游戏，因此多人能力需要新增实时同步层，但不应破坏现有单人可试玩闭环。

决策：

- 多人 v1 默认进入公共房间 `public-mars-alpha`。
- 浏览器首次进入生成匿名 `playerId`，保存在 `localStorage`；同一浏览器刷新后继续使用同一身份。
- 不使用 IP 作为玩家身份主键。IP 只可作为服务器日志、限流或防刷辅助，因为同一网络、VPN、校园网和公司网络都会造成误判。
- 任务内容相同，但进度各自独立；一个玩家完成任务不会推进其他玩家。
- 同步内容只包括远程宇航员的位置、朝向、移动速度、飞行状态和室内/室外状态。
- 远程玩家不可碰撞、不可互动，不参与任务触发。
- WebSocket 服务不可用时，前端安静降级为单人体验。

实现：

- 新增 `server/multiplayer.ts` 作为 Node WebSocket 房间转发服务。
- 新增 `src/multiplayer.ts` 作为前端多人客户端。
- 新增 `src/multiplayer-protocol.ts` 共享消息类型。
- `package.json` 新增 `dev:server` 和 `dev:multi`。

### 远处太阳改为实体球

原因：

原来的太阳使用 canvas 渐变 Sprite，远处看像一团朦胧光斑，不符合当前希望看到明确太阳球体的视觉要求。

决策：

- 太阳视觉从 `THREE.Sprite` 改为 `THREE.SphereGeometry` 实体球。
- 保留一层很薄的外发光壳用于亮度，但主体轮廓必须清楚。
- 风暴时只降低太阳实体和外发光透明度，不再回到大面积雾状光斑。

### HUD 增加在线人数和 Alex 职位

原因：

多人 v1 已经能在同一公共房间显示远程宇航员，左侧人员指标需要让玩家知道当前同时在玩的人数；Alex 下方原来的“在线”容易和在线人数重复。

决策：

- 人员 HUD 中 Alex 下方显示职位 `巡航员`。
- 人员 HUD 增加 `在线人数`，格式为 `1人`、`2人`。
- 在线人数口径为本地玩家 `1` 加当前房间远程玩家数量；WebSocket 不可用时仍显示 `1人`。

### 互动菜单最多显示两个选项

原因：

大屏幕底部同时出现三个互动按钮时，第三个选项会落到下一行，玩家无法用当前 `Q/E` 双键机制稳定选择，也容易遮挡画面。

决策：

- 同一位置的互动菜单最多显示两个选项。
- 显示前先按优先级排序：Mother 呼叫、进出建筑/升降梯、当前任务、机器人/福福、氧气补给。
- 超出两个的低优先级互动暂时隐藏；玩家离开重叠区域或条件变化后仍可重新出现。

### 黑色方碑埋入地面并允许缩放太阳

原因：

黑色方碑底部的圆形暗盘看起来像额外底座，不符合“方碑直接埋在火星土里、通过太阳投影产生压迫感”的方向；同时缩放枪既然是玩家获得的异常工具，远端实体太阳也应该能被瞄准并放大或缩小。

决策：

- 黑色方碑不再使用圆形接触盘或底座，保留直立黑色矩形主体和细边线，视觉上直接插入地面。
- 方碑与太阳之间的画面关系依赖场景光照、投影和背光构图，不再用假圆形阴影模拟压迫感。
- 太阳加入缩放枪可锁定目标，准星显示为 `太阳`。
- 缩放枪射程提高到能覆盖远端太阳实体；太阳缩放仍遵守当前 `0.25x` 到 `4x` 与 60 秒自动恢复规则。

### 缩放枪瞄准时锁定角色移动

原因：

缩放枪进入瞄准态后继续让 `WASD` 控制人物移动，会让玩家一边瞄准一边误走位，尤其是瞄远处太阳或黑色方碑时不稳定。当前键位方向已经强调单手操作，因此 `WASD` 在瞄准态应成为准星控制，而不是移动控制。

决策：

- 按 `X` 打开缩放枪后，Alex 停止移动；必须再次按 `X` 收起缩放枪后才能恢复走动。
- 缩放枪瞄准态下，`W/A/S/D` 改为控制准星方向：`A/D` 左右转向，`W/S` 上下调整。
- 鼠标锁定状态下仍可辅助瞄准，但键盘 `WASD` 是桌面端默认瞄准方式。
- 瞄准态下 `Q/E` 仍分别负责缩小/放大，不触发普通互动。

### 太阳视觉改为火球而非月亮

原因：

远处太阳改成实体球后，第一版颜色太淡、光晕太弱，看起来像一轮明月，不符合“太阳是大火球”的直觉。

决策：

- 太阳保留实体球轮廓，但表面改为黄白、橙红和深红混合的火纹贴图。
- 外层增加橙红燃烧壳、日冕和更明显的外光晕。
- 火球核心、外壳和日冕加入轻微旋转与闪烁，让太阳有燃烧感。
- 光晕必须服务于火球轮廓，不能把太阳重新做成无结构的大雾团。

### 奖励与随机金币系统 v1

原因：

当前任务完成、建筑探索和隐藏发现缺少即时正反馈。项目后续会继续增加任务，如果每个任务都单独写奖励逻辑，容易失控；同时用户希望先建立金币积累展示，为未来购物系统预留货币基础。

决策：

- 右上角 `F` 地图按钮左侧新增奖励 HUD，顺序为 `金币`、`积分`。
- 积分分为任务积分、探索积分和搞笑积分；主线默认 `+100`，支线默认 `+40`，建筑探索默认 `+10`，隐藏问号目标默认 `+30`，搞笑事件默认 `+10`，负面搞笑事件默认 `-10`。
- 奖励不写进对话或任务提示，只在玩家做到后即时发放，并配合数字放大、发光和音效。
- 用缩放枪放大或缩小太阳属于搞笑积分事件。
- 金币独立于积分，同一时间只存在一组随机金币，每组 10 枚，沿地表排成一列，按真实游玩时间 4 小时刷新或迁移。
- 金币视觉只保留金币本体，去掉每枚金币外面的圈。
- 金币生成必须避开 `world.colliders` 中的固体；雷达只显示一个金币组中心标记。
- 玩家穿过每枚金币时播放“叮”，金币从 3D 位置飞向右上角金币数值，到达后增加金币数。
- 氧气和体力不足导致的重生暂不扣积分、不扣金币。

### 第三人称防遮挡与升降台站立面修复

原因：

居住舱等大型建筑贴近玩家时，默认第三人称相机会落在建筑后方，画面可能被建筑模型挡满，玩家完全看不见 Alex。飞船升降台下落时，旧逻辑用手填 `surfaceY` 放置角色，没有读取平台地板的真实顶面，模型缩放和平台厚度会导致角色视觉上陷入平台。

决策：

- 室外第三人称相机增加基于 `world.colliders` 的视线遮挡检测；玩家到相机的连线穿过固体时，镜头沿地表法线平滑抬高，遮挡解除后自动回落。
- 第一人称、室内、飞船内舱和缩放枪瞄准态不启用该防遮挡抬镜头，避免干扰专用视角。
- 飞船升降台在 `ElevatorControl` 中记录真实地板对象 `surfaceObject`。
- Alex 站在升降台上时，以 `surfaceObject` 几何包围盒的顶面作为站立基准，并增加少量鞋底余量，避免上行或下行时陷入平台模型。

### 火星足球与轻量球面物理

原因：

需要在基地星球上放置一个一眼能看出是足球的可玩物体，并让 Alex、机器人、火星车和运输车碰到它时产生不同力度的滚动、飞行和反弹。项目当前没有完整刚体物理引擎，直接引入物理引擎会扩大复杂度和包体。

决策：

- 足球放在居住舱外的基地可见区域，作为可被玩家和活动物体推动的场景物体。
- 外观使用程序化 Canvas 贴图：白色球体、黑色足球拼块、缝线、明暗层次和 bump 细节，不新增外部贴图资产。
- 运动采用轻量球面物理：足球位置保存为星球法线、离地高度、地表切线速度和竖直速度。
- 竖直方向使用火星重力 `3.71 m/s²`；地表方向按 `PLANET_RADIUS` 转换为小星球表面角位移，并加入滚动摩擦和空气阻力。
- Alex 碰球时按 `playerVelocity` 的接触方向分量给冲量；加速跑会比普通行走踢得更远。
- 机器人、火星车和运输车碰球时用上一帧和当前帧的位置差估算实际速度；运输车/火星车的冲量高于维修机器人。
- 足球撞到建筑、飞船、岩石等静态 `world.colliders` 时按碰撞法线反弹并损失能量；撞到活动物体时按相对速度做动态反弹。
- 足球出生点穿过星球直径的另一面放置一个球门；优先使用严格对跖点，如果该点和已有碰撞体冲突，就在附近按切线圈搜索安全位置。
- 球门朝向足球出生点，进球判定使用球门本地宽度、高度和球网深度范围。
- 足球进入球网后每次奖励 `+100` 积分，随后立即重置到出生点，不保留在球网里；进球有短暂冷却，避免同一帧重复计分。

### 建筑探索自动计分与机器人外置巡逻

原因：

建筑探索属于“玩家已经到达”的事实，不需要再让玩家确认是否探索；额外按钮会干扰任务、进门和机器人通话。温室维修机器人仍然可能在视觉上陷入温室模型，说明机器人巡逻点和建筑碰撞体之间的安全余量不够。

决策：

- 建筑探索积分改为进入建筑互动范围后自动发放。
- 不再显示“探索 XX 建筑”互动按钮，也不再为建筑探索积分弹出浮动提示；右上角积分数值仍会更新。
- 当前任务互动仍可顺手触发建筑探索计分，但不要求玩家额外确认。
- `02 机器人 温室维修工` 的出生和巡逻点外移到温室正门外更远区域。
- 机器人巡逻点避障余量加大，避免数学碰撞体外但视觉上仍被建筑埋住或困住。

### 飞船出舱到升降梯落地前保持正面镜头

原因：

从飞船内舱出来后会启用人物正面镜头，但旧逻辑按玩家距离舱门原点释放镜头。升降梯下行时，玩家虽然还在平台上，和舱门原点的距离会快速变大，导致下行一半突然切回默认第三人称跟随视角，观感突兀。

决策：

- `exitFrontCamera` 仍可按距离释放，用于居住舱、温室和普通出舱后的移动恢复。
- 如果玩家正在乘飞船升降梯下行，正面镜头不按距离释放。
- 直到升降梯到达地面、`ridingElevator` 结束后，再恢复默认第三人称跟随镜头。

### 首页语言切换与游戏内锁定语言

原因：

游戏需要面向中文和英文玩家，语言入口应在首页第一屏可见。进入游戏后再切换语言会打断游戏状态和沉浸感，也可能让运行时按钮、任务文本与当前交互状态不同步，所以语言选择只在首页开放。当前项目文字散落在 HTML、HUD、任务提示、互动菜单、地图标签和对话数据中，需要先建立统一的轻量本地化管线。

决策：

- 右上角新增语言切换按钮，默认语言为 `简体中文`。
- 中文模式下按钮显示 `English`；英文模式下按钮显示 `简体中文`。
- 语言切换按钮只在首页显示；进入游戏后隐藏，且逻辑层会忽略新的切换请求。
- 语言选择保存到 `localStorage["mars.language"]`，刷新后保留上次选择；首次进入仍默认中文。
- 首页静态文案、HUD、任务条、奖励读数、地图按钮、互动菜单、退出确认、操作指南和缩放枪 UI 使用 i18n key 或静态文本扫描器刷新。
- 运行时任务、短提示、标签、地图显示和对话舞台通过 `localizeText()` 做英文显示；中文文案仍作为项目权威源文本保留。
- 长剧情对白后续可继续逐条补充更自然的英文精翻，不影响当前中文默认体验。
- 标签创建阶段只保存中文源文本，不调用语言系统；等 i18n 初始化完成后再统一刷新标签，避免页面加载阶段因初始化顺序导致脚本中断。
- 新增 `pnpm i18n:audit` 作为双语缺口检查。它会检查 HTML i18n key 的中英完整性，并把新增中文源文本和当前 baseline 对比，提示是否需要补英文词表或翻译表。
- 当前已有中文剧情和源文本写入 `scripts/i18n-audit-baseline.json`，避免历史存量噪音淹没真正新增的缺口；确认某批新增中文暂不翻译时，才运行 `pnpm i18n:audit:update` 更新基线。

### 碰撞触发探索积分

原因：

建筑探索原本只按交互点半径自动发放。飞船和大型建筑的可见模型、物理阻挡范围与交互点半径不完全一致，玩家可能已经撞到模型并被挡住，但还没有进入探索判定半径，导致积分不变。

决策：

- 保留进入建筑互动范围自动给分的逻辑。
- 在玩家与 `world.colliders` 中的建筑、飞船、太阳能阵列等实体发生首次碰撞时，也触发对应探索积分。
- 建筑碰撞复用原建筑探索记录，仍只奖励一次，并继续参与“全部建筑探索完成”统计。
- 飞船和太阳能阵列碰撞使用独立一次性探索事件，不影响建筑总完成数。

### 火星尺度扩大 50%

原因：

随着飞船、建筑、机器人、足球和球门等内容增加，原星球表面显得偏拥挤。如果只放大星球半径、不展开布局坐标，设施之间的实际地表距离几乎不会改变；如果把全部模型一起缩放或放大，又会破坏已有交互半径、碰撞和视觉比例。

决策：

- 新增 `WORLD_EXPANSION = 1.5`，`PLANET_RADIUS` 从 `88` 调整为 `88 * 1.5`。
- 基地布局展开系数从 `2` 调整为 `2 * 1.5`，保持设施相对方位关系，同时让设施间地表距离扩大 50%。
- 玩家出生点、足球出生点、补氧点和黑色方碑等非 `spread()` 坐标通过 `expandWorldCoordinate()` 同步展开。
- 建筑、飞船、机器人、车辆、足球、碰撞半径和交互半径保持原尺寸，不随星球半径放大。

### 建筑外壳碰撞补强与足球手感加速

原因：

居住舱是长条舱体，旧碰撞体只用一排圆形 collider 近似，正面和侧面存在视觉模型已挡住、物理 collider 没覆盖的漏区。Alex 可以从非门区穿入舱体，足球也会因为复用同一套 `world.colliders` 而滚进舱内消失。其他矩形建筑如果继续只用单圆，也会有类似风险。足球在火星扩大后显得滚动距离偏短，游戏感不足。

决策：

- 新增 `addFootprintColliders()`，用多排圆形 collider 覆盖长条舱体和矩形建筑外壳。
- 居住舱、温室、氧气生产站、甲烷燃料厂、机器人车库、科研舱、物资仓和医疗舱都改为更连续的足迹碰撞。
- 居住舱和温室在玩家进入室内状态时关闭外部外壳碰撞；室外进入仍通过互动按钮完成，不允许从模型表面直接走进去。
- 足球静态反弹继续使用 `world.colliders`，因此建筑外壳补强会同时阻止足球进入建筑模型。
- 足球玩家/车辆/机器人碰撞冲量乘以 `2.0`，最高速度从 `16.5` 提到 `33`，并降低滚动与空气阻力，让火星足球表现更夸张、滚得更远。

### 流星尾迹雾化与近火掠过轨道

原因：

旧流星尾迹使用 `THREE.Line` 三点线段，视觉上像一根长天线，而且对反向轨道的流星不一定跟随真实运动方向。后续雾团版本虽然去掉了线条，但主体过白、像自发光球，尾迹也像一个个光圈，不像火星车粉尘那种真实雾态。用户希望陨石本体是黑白灰石质，不自发光，只在受到光照时可见；尾迹更接近粉尘雾。

决策：

- `Meteor.trail` 从 `THREE.Line` 改为 `THREE.Group`，内部使用多个 `SpriteMaterial` 雾片作为尾迹，复用火星车粉尘的 `getDustFogTexture()`，不再使用发光球形光圈。
- 新增低精度陨石贴图 `src/assets/meteor-rock-generated.png`，由 Image 生成后压缩到 `128x128`，当前大小约 `26KB`。
- 陨石主体从 `MeshBasicMaterial` 改为 `MeshStandardMaterial`，使用灰黑贴图作为 `map` 和 `bumpMap`，不再自发光；远处只通过缩放和透明度弱化，近处靠场景光照显示坑洼石质。
- 每帧用当前轨道位置和前方采样点计算真实运动方向，尾雾沿运动反方向排列，避免尾迹方向错误。
- 三颗流星仍保留，概念上都按太阳轨道上的远处天体处理，不作为火星卫星绕火星旋转。
- 第 1 颗设为周期性近火交会路径，每现实 `180` 秒掠过火星一次；最近点抬高到 `PLANET_RADIUS + 105`，明显高于飞船高度两倍以上，远处退到 `PLANET_RADIUS + 1550`，远处缩成星点，接近时主体放大到接近飞船量级。
- 近火陨石尾雾使用更多、更大、更高透明度的 `SpriteMaterial` 雾片，形成更浓密的气体包裹和拖尾，但仍避免线条、光圈或自发光太阳感。
- 远处两颗不掠过火星，始终保持在 `PLANET_RADIUS + 980` 以上的远距离天空区域，以 7 到 11 分钟左右的慢周期做随机椭圆漂移，像远处行星或小星体。

### 职位晋级系统绑定积分和正式任务

原因：

用户希望 Alex 从 `实习巡航员` 开始，并在完成任意任务后成为 `初级宇航员`；同时职位需要和积分系统建立关系，但金币不应干扰职位资历。

决策：

- 人员 HUD 中 Alex 下方不再显示固定 `巡航员`，改为动态职位。
- 初始职位为 `实习巡航员`。
- 完成任意正式任务，或积分达到 `100`，晋级为 `初级宇航员`。
- 后续职位按积分门槛晋级：`300` 正式宇航员，`700` 高级宇航员，`1500` 火星任务专家，`3000` 火星基地副指挥官。
- 最高称号 `第一位居民` 暂以主线完成且 Mother 信任、基地状态较高作为解锁条件，不靠刷分。
- 职位只升不降；负面搞笑扣分不会降级。
- 金币作为未来消费货币，不参与职位晋级。

### NASA Perseverance 火星车纪念点

原因：

用户希望火星表面出现现实中已经抵达火星的 NASA 火星车，增强真实感。NASA 官方资料显示 Perseverance 位于 Jezero Crater，任务与古河流三角洲/湖泊环境、取样和火星地质探索相关；外观上有六轮、桅杆相机、仪器箱和机械臂等高识别特征。

决策：

- 在游戏星球上新增静态地标 `NASA 火星车 Perseverance / Jezero Crater`。
- 模型使用 Three.js 几何体低多边形拼装，不引入外部 NASA 模型；重点保留六轮、摇臂支架、桅杆相机、车身仪器箱、后部 RTG 圆筒和机械臂/钻头。
- 它作为真实感纪念点和地图标签存在，不加入动态巡逻车辆系统，也不影响主线任务。
- 地点放在基地外侧空旷区域，作为 Jezero Crater 的游戏化代表位置，避免与现有建筑、飞船、足球玩法重叠。

### 神秘物体雷达与隐藏发现统一规则

原因：

雷达此前只有零散的 `unknown` 判断，黑色方碑等少数目标会显示问号，但足球、NASA Perseverance 火星车这类非基地常规设施没有统一归类。后续继续增加异常物、纪念物或外来物体时，如果每个目标单独写雷达和积分逻辑，容易出现显示不一致或遗漏奖励。

决策：

- 神秘物体统一包括足球、NASA Perseverance 火星车、黑色方碑、未知生命和坠毁/未编号异常物体。
- 基地建筑、飞船升降台、太阳能阵列、普通机器人、普通车辆、氧气补给点、金币和主线任务目标不归入神秘物体。
- 雷达上的神秘物体始终用 `?` 图标表示。
- 未发现前，展开雷达只显示“未知物体”或“未知生命迹象”，不泄露真实名称。
- 玩家首次接近神秘物体，或首次碰撞足球时，奖励隐藏发现积分；同一目标只奖励一次。
- 发现后，展开雷达继续保留 `?` 图标，但标签显示真实名称，例如“火星足球”或 “NASA 火星车 Perseverance / Jezero Crater”。

### 基地亮面集中布局

原因：

火星放大后，主要建筑、飞船和太阳能阵列在首页视角里显得过于分散，不像一个工程基地。最初基地概念图的组织方式更接近“居住舱中心 + 温室/工业/车库/通信/飞船/太阳能阵列围绕”的紧凑组团，因此需要只移动大型基地设施，让首页亮面看起来像完整基地。

决策：

- 01-09 号主要建筑、01-03 号飞船和 A/B/C 太阳能阵列集中到首页亮面。
- 居住舱作为中心，温室和氧气站靠近生活核心，甲烷燃料厂、车库、科研舱、物资仓、医疗舱围绕外侧。
- 登陆飞船、货运飞船和返回飞船放在基地外围安全距离；01/02 飞船保持当前合理间距并作为一组远离太阳能阵列和医疗舱，03 返回飞船进一步远离基地和另外两艘飞船，保留升降梯和 Elon 支线空间。
- 太阳能阵列集中到亮面直射区域，三组平行排成一条赤道式电力场，间距一致，并远离货运飞船和医疗舱。
- 根据首页实际受光画面，太阳能阵列从背光侧旧位置 `(44,-70) / (64,-70) / (84,-70)` 先移到 `(48,54) / (66,54) / (84,54)`；后续根据截图修正为更靠近 03 返回飞船一侧的亮面区域 `(-22,52) / (-4,52) / (14,52)`，并同步更新连接管线到阵列 C 的端点。
- 同步更新基地管线和氧气补给点坐标，避免视觉连接与补给雷达指向旧位置。
- 坠毁飞船残骸和福福救援点不移动，继续留在暗面；黑色方碑、足球、球门、NASA Perseverance 火星车、岩石、金币刷新逻辑和彗星/陨石也不移动。
- 模型尺寸、碰撞半径和交互半径不变，只改变大型基地设施的位置。

### R 任务按钮与地图互斥规则

原因：

移动端屏幕空间有限，当前任务卡和互动面板、地图展开态容易互相遮挡。用户希望任务提示从固定卡片改成 `R` 按钮控制，并且在移动端 `R` 与 `F` 两个展开面板只能出现一个；桌面端空间充足，任务和地图可以同时打开。

决策：

- 游戏开始后，`V` 右侧显示 `R` 任务按钮，桌面端和移动端都通过它展开/关闭当前任务。
- 首次进入游戏时任务卡短暂展开后自动收起到 `R`，让玩家理解按钮用途。
- 有新任务提示时，`R` 外框发光；玩家打开任务卡后清除提醒。
- 移动端打开 `R` 时自动收起 `F` 地图，打开 `F` 时自动收起 `R` 任务卡。
- 桌面端允许 `R` 任务卡和 `F` 地图同时打开。
- 首页语言按钮右边缘与游戏内 `F` 按钮右边缘对齐，保持入口页和游戏 HUD 的右侧基准一致。

### 删除头盔灯/照明功能

原因：

`R` 已改为任务面板入口，移动端也需要减少右下角按钮负担。继续保留“照明/头灯”会增加按键冲突和 HUD 复杂度。

决策：

- 移除移动端“照明”按钮。
- 移除玩家头盔灯 SpotLight、地面光斑、开关状态和每帧更新逻辑。
- 移除多人同步协议里的 `lampOn` 字段和远程玩家头灯光源。
- 操作指南和技术方案不再声明任何头盔灯快捷键。

### 飞行模式降落与空中动画修正

原因：

作弊码飞行模式下，`Control/Ctrl` 下降被最低飞行高度卡住，无法真正落地；空中移动仍复用走路步态，看起来像在天上跑步；从高处降到飞船走廊或升降台区域时也缺少可行走落点判定。

决策：

- 飞行下降允许高度降到 `0`，到火星地表后自动关闭飞行模式、清除上升/下降键并隐藏飞行喷焰。
- 飞行中 Alex 不再播放走路摆腿，改为手臂和双腿轻微后摆，表现飞行背包推动时身体被气流拖住的姿态。
- 飞行下降时检测三艘飞船升降台顶部和高处廊道的 `walkBounds`；如果玩家落在可行走范围内，会吸附到该平台并切换为正常步行/升降台行走状态。
- 该落点吸附只在升降台到达顶部且未运动时生效，避免玩家在运动平台中途穿模。

### V 按钮开关态视觉统一

原因：

HUD 的 `V` 按钮原先在关闭信息时反而显示高亮，和 `F` 地图按钮“打开才有色”的视觉规则相反，容易让玩家误判当前状态。

决策：

- `V`、`R`、`F` 三个顶部圆形按钮关闭态统一使用半透明深色底。
- 对应信息打开时，按钮不填色、不加粗外圈，改用静态红色光晕表示打开状态。
- 只有 `R` 在有新任务信息时允许动态脉冲发光；打开任务面板后，动态脉冲停止，保留静态发光直到关闭。
- 顶部按钮禁用浏览器默认蓝色焦点框，焦点态也纳入红色光晕体系，确保只出现不发红光、静态红光和动态红光三种状态。

### 金币与积分 HUD 按钮化

原因：

右上角金币和积分原先是带中文标签的纯文本，和 `V`、`R`、`F` 三个顶部按钮的视觉体系不一致；移动端宽度也更紧张，需要减少文字长度。

决策：

- 金币和积分保留在 `F` 左侧，改为同 HUD 按钮体系的圆角矩形。
- 金币不显示“金币”文字，初始显示 `$`；获得金币后显示三位数字，例如 `001`。
- 积分不显示“积分”文字，初始显示 `0`；获得积分后显示三位数字，例如 `001`。
- 金币按钮颜色和左侧 HUD “基地”标题一致；积分按钮颜色使用顶部按钮红色光晕色系。
- 金币/积分按钮的边框透明度、深色底、胶囊圆角和字体厚度沿用首页语言切换按钮，但不改变金币黄和积分红。

### 剧情对话框响应宽度

原因：

大屏幕下剧情对话框接近横向拉满，阅读区域显得过宽，也压迫画面空间。用户希望宽屏约占屏幕宽度 70%，小屏幕可以更宽到 90%。

决策：

- 剧情对话舞台新增 `--dialogue-panel-width` 变量。
- 桌面和宽屏使用约 `70vw`，并通过 `clamp()` 保留最小可读宽度和左右安全边距。
- 小屏幕 `max-width: 700px` 时切换为约 `90vw`。
- 小屏幕对话框固定高度并向上抬，不再随内容收缩或撑开；正文区域最多三行，超过三行由运行时自动分页，选项/继续按钮固定跟在正文区域下方。
- `aria-pressed` 语义同步调整为表示“HUD 信息正在显示”，而不是表示“HUD 已折叠”。
- `V` 折叠态不再强制隐藏 `R` 任务面板，避免玩家点击发光的 `R` 后任务已打开但被 HUD 折叠规则遮住。

### 互动按钮窄宽度与圆角统一

原因：

移动端互动入口原先横向占位偏宽，在人物靠近左侧 HUD 区域时容易碰到或遮挡状态文字；桌面端互动选项也需要与当前 HUD 按钮体系更一致。

决策：

- 移动端互动入口改为居中窄宽度，按屏幕宽度响应式变化，最大不超过中等按钮宽度。
- 互动入口和桌面互动选项统一为轻微圆角矩形，保持蓝色描边和深色半透明底。
- 展开的移动端互动抽屉仍保留底部面板形态，但其中选项按钮沿用圆角矩形风格。

### 移动端双选项对话 A/B 模式

原因：

小屏对话框固定高度后，两个长选项纵向堆叠会挤占正文区域，第二个按钮容易掉出面板。用户希望按钮固定在一行，并列成两个短按钮。

决策：

- 移动端小屏对话只有两个选项时，正文自动追加 `A.` / `B.` 两行选项说明。
- 底部选择按钮固定一行两列，只显示 `A` 和 `B`，并通过 `aria-label` 保留完整选项含义。
- 桌面端和三个以上选项继续显示完整按钮文案，避免信息被过度压缩。

### 对话按钮音效反馈

原因：

剧情对话按钮原先只有视觉变化，点击后缺少和首页按钮一致的即时反馈。用户希望选择对白时发出与“进入基地”相同的短促滴声。

决策：

- 对话选项确认、继续翻页、推进对白和关闭对白统一调用 `playUiBeep()`。
- 音效放在对话动作函数内部，而不是只绑定鼠标点击，确保键盘、触摸和点击反馈一致。
- 无效确认不播放音效，避免玩家按错键时产生误导。

### ARES 火星历动态日期

原因：

网站首页已经上线，标题上的 “2050 年 / 第 30 火星日” 不应再作为静态装饰。用户希望它能随真实时间推进，同时保留硬科幻可信度。

决策：

- 采用 NASA Mars24 使用的平均火星太阳日长度 `88775.244` 秒作为一个 sol。
- 以 `2026-06-27 00:00 Asia/Shanghai` 为 ARES 基地民用历上线锚点，对应 `2050 年 / 第 30 火星日`。
- 之后每经过一个真实 sol，显示 sol 数 +1；火星日长于地球日，所以日期跳转点每天会比北京时间午夜晚约 `39分35秒`。
- 火星年显示采用硬科幻民用历规则：每 `669` 个 sol 进位一年。该值来自火星年约 `687` 个地球日，折算约 `668.6` 个 sol 后取整。
- 中英文首页日期共用同一套计算，中文显示 `{year} 年 / 第 {sol} 火星日`，英文显示 `Year {year} / Sol {sol}`。

### 首页语言切换改为国旗圆形按钮

原因：

首页右上角原本的文字语言按钮和游戏内 HUD 顶部圆形按钮体系不一致，视觉上显得像网页控件。

决策：

- 首页语言切换按钮改为与游戏内 `F` 按钮相同的位置、尺寸、圆形外框和 hover/focus 发光样式。
- 按钮不再显示 `English / 简体中文` 文字，改为圆形裁切的局部国旗图案。
- 中文界面显示美国国旗，表示点击切换到 English；英文界面显示中国国旗，表示点击切回简体中文。
- 国旗由 CSS 绘制，统一放大和裁切比例，优先展示左上角识别区域，避免新增图片资源。
- 后续视觉调整中，语言按钮进一步缩小为约 `24px` 的立体自转小球：保留点击逻辑，使用国旗 CSS 贴图、明暗高光和阴影模拟球体。

### 三次压缩后的新对话交接机制

原因：

长对话多次自动压缩后，容易出现上下文细节损耗、响应变慢和误读旧状态。项目已经采用“聊天只是过程，项目文件才是共同记忆”的协作原则，因此需要一个明确的触发点来检查记录完整性，并建议切换到新对话。

决策：

- 同一项目对话发生约 3 次自动压缩后，默认进入“交接检查”状态，不继续默认推进大型修改。
- 交接检查重点确认技术/UI 决策、重要取舍、剧情/设定/任务和当前实现状态是否已经写入项目文件。
- 新增 `docs/dev-log/current-handoff.md` 作为当前交接入口；若记录完整，建议开启新对话继续；若记录不完整，先补交接摘要或对应权威文档。
- 新对话恢复上下文时优先读取 `AGENTS.md`、`docs/project-management.md`、`docs/production-blueprint.md`、`docs/dev-log/current-handoff.md`、`docs/tech-plan.md` 和 `docs/decision-log.md`，不依赖旧聊天摘要。
